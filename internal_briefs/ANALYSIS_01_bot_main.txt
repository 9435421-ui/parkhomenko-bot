АНАЛИЗ ОСНОВНОГО ФАЙЛА БОТА bot_unified.py
==========================================

1. КОЛИЧЕСТВО СТРОК В ФАЙЛЕ:
   1524 строки

2. РЕЖИМЫ РАБОТЫ БОТА (class BotModes):
   - QUIZ = "quiz"     # Режим квиза/заявки
   - DIALOG = "dialog" # Диалоговый режим консультации
   - QUICK = "quick"   # Быстрая консультация

3. ДОСТУПНЫЕ КОМАНДЫ ПОЛЬЗОВАТЕЛЯМ:
   Все команды, найденные в @bot.message_handler(commands=[...]):
   - /start          # Запуск бота и инициализация
   - /privacy        # Информация о политике конфиденциальности
   - /test_gpt       # Тестирование GPT (только для администратора)
   - /test_rag       # Тестирование RAG (только для администратора)
   - /generate_content    # Генерация контент-плана (только для администратора)
   - /add_subscriber      # Добавление подписчика (только для администратора)
   - /list_birthdays      # Список дней рождения (только для администратора)
   - /generate_greetings  # Генерация поздравлений (только для администратора)
   - /generate_welcome    # Генерация приветствий (только для администратора)
   - /show_plan           # Показ контент-плана (только для администратора)

4. INLINE-КНОПКИ ПОСЛЕ /start:
   После команды /start пользователям показывается главное меню с одной inline-кнопкой:
   - "📝 Оставить заявку" (callback_data="mode_quiz")

   Это происходит в функции show_main_menu(), которая вызывается после успешной инициализации пользователя.

ДОПОЛНИТЕЛЬНЫЕ ЗАМЕЧАНИЯ:
- Файл содержит около 1500+ строк кода
- Реализованы 3 основных режима работы бота
- Большинство команд предназначены только для администратора
- Пользователям доступны только базовые команды: /start, /privacy
- После /start показывается минималистичный интерфейс с одной основной кнопкой для оставления заявки
