# 🚀 Работа с Git в Cursor

## Использование встроенного Git интерфейса Cursor

### Способ 1: Source Control панель (Рекомендуется)

1. **Откройте Source Control:**
   - Нажмите `Ctrl+Shift+G` или кликните на иконку Git в боковой панели слева
   - Или через меню: `View` → `Source Control`

2. **Просмотр изменений:**
   - Все измененные файлы будут показаны в списке
   - Кликните на файл, чтобы увидеть diff (различия)

3. **Добавление файлов в staging:**
   - Нажмите `+` рядом с файлом или `+` рядом с "Changes" для всех файлов
   - Или используйте кнопку "Stage All Changes"

4. **Создание коммита:**
   - Введите сообщение коммита в поле сверху
   - Нажмите `Ctrl+Enter` или кнопку "Commit"
   - Или используйте иконку галочки ✓

5. **Push изменений:**
   - После коммита появится кнопка "Sync Changes" или "Push"
   - Нажмите на неё для отправки в GitHub
   - Если нужно настроить credentials, Cursor предложит это сделать

### Способ 2: Command Palette

1. Нажмите `Ctrl+Shift+P` (или `F1`)
2. Введите команду:
   - `Git: Commit` - создать коммит
   - `Git: Push` - отправить изменения
   - `Git: Pull` - получить изменения
   - `Git: Stage All Changes` - добавить все файлы

### Способ 3: Терминал Cursor

1. Откройте встроенный терминал: `` Ctrl+` ``
2. Используйте стандартные Git команды:
   ```bash
   git add .
   git commit -m "описание изменений"
   git push origin main
   ```

## 🔐 Настройка Git Credentials в Cursor

### Если Cursor запрашивает учетные данные:

1. **Через встроенный интерфейс:**
   - При первом push Cursor предложит авторизоваться через GitHub
   - Следуйте инструкциям в диалоговом окне

2. **Через настройки:**
   - `File` → `Preferences` → `Settings` (или `Ctrl+,`)
   - Найдите "Git: Authentication"
   - Настройте GitHub authentication

3. **Через Git Credential Manager:**
   - Windows: Cursor использует Windows Credential Manager
   - При первом push введите логин и Personal Access Token (PAT)
   - Cursor сохранит их автоматически

### Создание Personal Access Token (PAT) для GitHub:

1. Перейдите на GitHub: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен (он показывается только один раз!)
5. При push в Cursor используйте:
   - Username: ваш GitHub username
   - Password: вставьте PAT (не ваш пароль!)

## 📋 Текущие изменения для коммита

Следующие файлы готовы к коммиту:

- ✅ `services/lead_hunter/discovery.py` - исправление логики SCOUT_KEYWORDS
- ✅ `.vscode/settings.json` - настройки репозитория
- ✅ `REPOSITORY.md` - документация
- ✅ `scripts/git_push.ps1` - скрипт для быстрого пуша

## 🎯 Рекомендуемое сообщение коммита:

```
fix(discovery): игнорировать SCOUT_KEYWORDS если менее 5 слов + настройки репозитория
```

## ⚡ Быстрые действия

1. **Откройте Source Control:** `Ctrl+Shift+G`
2. **Добавьте все изменения:** нажмите `+` рядом с "Changes"
3. **Введите сообщение коммита:** в поле сверху
4. **Создайте коммит:** `Ctrl+Enter` или ✓
5. **Запушьте:** нажмите "Sync Changes" или "Push"

---

**Совет:** Если Git не работает, проверьте:
- Установлен ли Git: `git --version` в терминале
- Правильно ли настроен remote: `git remote -v`
- Есть ли доступ к интернету
