# 💰 Telegram Mini App - Инвест-калькулятор

Веб-приложение для оценки потенциала квартиры под перепланировку и капитализацию.

## 📋 Описание

Mini App интегрируется с Telegram Bot и позволяет:
- Визуально рассчитать потенциальный прирост стоимости квартиры
- Оценить ROI (возврат инвестиций) от перепланировки
- Собрать данные для детальной консультации со специалистом

## 🚀 Деплой

### Локальный тест

1. Откройте `index.html` в браузере
2. Или запустите простой HTTP сервер:

```bash
python -m http.server 8000
# Откройте http://localhost:8000
```

### Деплой на хостинг

Рекомендуемые платформы:
- **Vercel** - бесплатный HTTPS
- **Netlify** - автодеплой из Git
- **GitHub Pages** - простой вариант
- **Cloudflare Pages** - быстрый CDN

### Подключение к боту

1. Загрузите файл на хостинг
2. Получите URL (например: `https://your-domain.com/mini_app/`)
3. В BotFather отправьте:
   ```
   /newapp
   /setappdomain - укажите ваш домен
   /setappphoto - загрузите иконку
   ```
4. В коде бота используйте:
   ```python
   from aiogram.types import WebAppInfo, InlineKeyboardButton
   
   button = InlineKeyboardButton(
       text="💰 Инвест-калькулятор",
       web_app=WebAppInfo(url="https://your-domain.com/mini_app/")
   )
   ```

## 🎨 Кастомизация

### Изменение формулы расчёта

Откройте `index.html` и найдите функцию `calculateInvestment()`:

```javascript
function calculateInvestment() {
    const area = parseFloat(document.getElementById('area').value);
    const budget = parseFloat(document.getElementById('budget').value);
    
    // Ваша формула здесь
    const priceIncrease = area * коэффициент + budget * множитель;
    
    // ...
}
```

### Изменение дизайна

Все стили находятся в блоке `<style>` в начале файла. Цвета автоматически адаптируются под тему Telegram.

## 📊 Отправка данных в бота

При клике на главную кнопку Mini App отправляет данные обратно в бота:

```javascript
tg.MainButton.onClick(function() {
    const data = {
        city: "Москва",
        area: 50,
        budget: 500000,
        // ... другие данные
    };
    
    tg.sendData(JSON.stringify(data));
    tg.close();
});
```

Бот получает их в handler'е:

```python
@router.message(F.web_app_data)
async def web_app_data(message: Message):
    data = json.loads(message.web_app_data.data)
    # Обработка данных
```

## 🔒 Безопасность

- ✅ Все вычисления на клиенте (быстро)
- ✅ Данные передаются через защищённый канал Telegram
- ✅ Нет сторонних зависимостей
- ✅ Адаптация под тему Telegram (light/dark)

## 📱 Поддержка платформ

- ✅ iOS (Telegram)
- ✅ Android (Telegram)
- ✅ Web (Telegram Web)
- ✅ Desktop (Telegram Desktop)

---

**Статус:** Заглушка готова к использованию  
**TODO:** Подключить реальные формулы расчёта от эксперта
