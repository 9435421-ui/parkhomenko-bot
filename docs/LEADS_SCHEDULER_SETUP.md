# Настройка планировщика сводок лидов

## Дата: 2026-02-17

## Реализованные функции

### 1. ✅ Отправка сводок обычных лидов по расписанию

**Расписание:** Трижды в день в 10:00, 14:00 и 19:00 МСК

**Логика:**
- Собираются лиды с `priority_score < 3` за последние 24 часа
- Исключаются лиды со стадиями ST-3 и ST-4
- Формируется сводка с превью текста, приоритетом и ссылками
- Отправляется в топик "Логи" рабочей группы

**Метод:** `hunter.send_regular_leads_summary()`

### 2. ✅ Немедленная отправка горячих лидов

**Триггеры для немедленной отправки:**
- HOT_TRIGGERS: "предписание МЖИ", "узаконить", "МЖИ", "штраф БТИ", "блокировка сделки", "суд по перепланировке"
- Стадии боли: ST-1, ST-2
- Высокий приоритет: `priority_score >= 3`

**Логика:**
- Горячие лиды отправляются сразу в топик "Горячие лиды" (THREAD_ID_HOT_LEADS)
- Отмечаются флагом `sent_to_hot_leads = 1` в БД
- Проверка неотправленных горячих лидов каждые 15 минут

**Метод:** `hunter.send_hot_leads_immediate()`

### 3. ✅ Обновленная логика process_lead

**Различение типов лидов:**

1. **Горячие лиды (HOT_TRIGGERS или ST-1/ST-2):**
   - Отправляются немедленно в топик "Горячие лиды"
   - Отмечаются в БД как отправленные

2. **Критические лиды (ST-3/ST-4):**
   - Отправляются только в рабочее время МСК (09:00-20:00)
   - Вне рабочего времени откладываются до 09:00

3. **Обычные лиды (priority < 3):**
   - Сохраняются в БД
   - Отправляются в сводке по расписанию (10:00, 14:00, 19:00 МСК)

## Изменения в коде

### database/db.py

**Добавлены методы:**
- `get_regular_leads_for_summary(since_hours=24)` - получение обычных лидов для сводки
- `get_hot_leads_for_immediate_send()` - получение горячих лидов для немедленной отправки
- `mark_lead_sent_to_hot_leads(lead_id)` - отметка лида как отправленного в топик "Горячие лиды"

**Миграция БД:**
- Автоматическое добавление колонки `sent_to_hot_leads` в таблицу `spy_leads`

### services/lead_hunter/hunter.py

**Обновлена логика process_lead:**
- Проверка на HOT_TRIGGERS в тексте поста
- Различение типов лидов (горячие, критические, обычные)
- Немедленная отправка горячих лидов
- Сохранение обычных лидов для сводки

**Добавлены методы:**
- `send_regular_leads_summary()` - отправка сводки обычных лидов
- `send_hot_leads_immediate()` - немедленная отправка горячих лидов

### main.py

**Добавлены задачи планировщика:**
- `send_regular_leads_summary_job` - отправка сводок в 10:00, 14:00, 19:00 МСК
- `check_and_send_hot_leads_job` - проверка горячих лидов каждые 15 минут

## Конфигурация

### Переменные окружения (.env)

```env
# Thread ID для топика "Горячие лиды"
THREAD_ID_HOT_LEADS=811

# Thread ID для топика "Логи" (для сводок обычных лидов)
THREAD_ID_LOGS=88

# ID рабочей группы
LEADS_GROUP_CHAT_ID=-1003370698977
```

## Расписание задач

### Отправка сводок обычных лидов:
- **10:00 МСК** (07:00 UTC) - утренняя сводка
- **14:00 МСК** (11:00 UTC) - дневная сводка
- **19:00 МСК** (16:00 UTC) - вечерняя сводка

### Проверка горячих лидов:
- **Каждые 15 минут** - проверка неотправленных горячих лидов

## Формат сводки обычных лидов

```
📋 Сводка обычных лидов (за последние 24 часа)
Всего лидов: 15

---

1. ЖК Зиларт
   Приоритет: 2/10 | Стадия: ST-1
   Можно ли объединить кухню с гостиной...
   🔗 Пост

2. Саларьево Парк
   Приоритет: 1/10 | Стадия: ST-1
   Подскажите, кто делал проект перепланировки...
   🔗 Пост

...
```

## Формат горячего лида

Горячие лиды отправляются в формате карточки с:
- Эмодзи 🚨 для ST-4
- Визуальной шкалой приоритета
- Кнопками действий: "✍️ На эту тему пост", "🛠 Ответить экспертно", "✅ Взять в работу"
- Ссылками на профиль и пост

## Тестирование

### Проверка отправки сводок:
1. Добавьте несколько обычных лидов в БД (priority_score < 3)
2. Запустите `hunter.send_regular_leads_summary()` вручную
3. Проверьте топик "Логи" на наличие сводки

### Проверка горячих лидов:
1. Добавьте горячий лид с HOT_TRIGGER или ST-1/ST-2
2. Запустите `hunter.send_hot_leads_immediate()` вручную
3. Проверьте топик "Горячие лиды" на наличие карточки

### Проверка планировщика:
1. Запустите бота: `python3 main.py`
2. Проверьте логи на наличие сообщений о добавлении задач
3. Дождитесь времени отправки сводки (10:00, 14:00 или 19:00 МСК)

## Зависимости

Для корректной работы timezone требуется:
```bash
pip install pytz
```

Если `pytz` не установлен, используется UTC с учетом смещения МСК (UTC+3).
