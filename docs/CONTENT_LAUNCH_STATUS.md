# Запуск контента — уточнение и текущее состояние

«Запуск контента» в полном виде означает:

1. **Генерация изображений** — обложки к постам по теме (Yandex Art / Router AI).
2. **Качество постов** — нормальные тексты и заголовки, без «кривых» формулировок и дублей.
3. **Контент-план** — посты с датой/временем публикации, статусами (черновик → одобрен → опубликован).
4. **Время публикаций** — явно заданное время (например 12:00 МСК) и при необходимости несколько слотов в день.
5. **Количество публикаций в день** — лимит и расписание (сколько постов в какой канал в день).
6. **Переход по ссылке в квиз** — есть (например `t.me/bot?start=quiz`).
7. **Квиз отвечает** — пользователь нажимает «Принимаю», отправляет контакт, отвечает на вопросы — бот должен на каждый шаг отвечать и переходить к следующему.

---

## Текущее состояние (по факту)

| Что | Статус |
|-----|--------|
| Генерация изображений | **Нет по факту** — код есть (Yandex Art + Router AI в `services/image_generator.py`), но либо ключи не заданы, либо API не отвечает/ошибки. Обложки по кнопке «🖼 Обложка» могут не генерироваться. |
| Посты кривые | Тексты и заголовки ещё требуют доработки (нормализация сделана частично: убраны дубли «1. 1.», но качество контента и шаблоны — на стороне контента). |
| Контент-план не работает | **Исправлено частично**: добавлена задача `check_and_publish_scheduled_posts`: каждый час и в 12:00 проверяются посты с `status='approved'` и `publish_date <= сейчас`, публикуются в каналы через `publisher`, затем помечаются как опубликованные. Чтобы посты выходили по расписанию, нужно при сохранении ставить им `publish_date` и переводить в `approved`. |
| Время для публикаций не прописано | Нет явной настройки «время публикации» (например 12:00, 18:00). При сохранении поста подставляется дефолт 12:00 в `add_content_post`, но отдельного конфига/настройки нет. |
| Количество публикаций в день | Не задано и не проверяется (нет лимита «не более N постов в день в канал»). |
| Переход по ссылке в квиз | **Работает** — `?start=quiz` открывает квиз (согласие с ПД). |
| Квиз не отвечает | **Исправлено**: диалог (F.text) раньше мог перехватывать сообщения в квизе и, делая `return` без ответа, «съедать» их. Для диалога добавлен фильтр **~StateFilter(QuizStates)** — в состоянии квиза обрабатывает только quiz_router, квиз должен отвечать на каждый шаг. |

---

## Что сделано в коде (этот коммит)

- **Квиз**: в `handlers/dialog.py` обработчик текста в диалоге теперь **не срабатывает** в состоянии квиза (`~StateFilter(QuizStates)`), чтобы ответы на шаги квиза обрабатывал только квиз.
- **Контент-план**: в `main.py` добавлена реальная задача публикации — `check_and_publish_scheduled_posts` (каждый час + в 12:00). Публикуются посты с `status='approved'` и `publish_date <= сейчас`.
- **Документ**: добавлен этот файл — что считать «запуском контента» и что сейчас не работает.

---

## Что остаётся сделать для полноценного запуска контента

1. **Генерация изображений** — проверить `YANDEX_API_KEY` / `FOLDER_ID` или `ROUTER_AI_IMAGE_KEY`, при необходимости поправить запросы к API (логи, обработка 400/401).
2. **Контент-план** — сделано: задача `check_and_publish_scheduled_posts` в планировщике. Дальше: настраивать `publish_date` при сохранении и переводить посты в `approved`.
3. **Время публикаций** — вынести в конфиг (например `PUBLISH_TIME="12:00"` или список слотов) и подставлять при сохранении поста и в очереди.
4. **Количество публикаций в день** — задать лимит (конфиг или БД) и при планировании/публикации не превышать его.
5. **Качество постов** — дорабатывать шаблоны и тексты вручную/через контент-процесс.

---
*Документ создан 16.02.2026.*
