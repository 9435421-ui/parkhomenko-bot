# Обновление LeadHunter v3.0 — Динамический ИИ-охотник

## Дата: 2026-02-17

## Выполненные изменения

### 1. ✅ Core Logic (hunter.py)

#### Удалена жесткая логика skip_count
- **Было:** Жестко закодированная логика пропуска первых N сообщений
- **Стало:** Инкрементальный поиск через `last_post_id` из БД для каждого ресурса
- **Файл:** `services/lead_hunter/hunter.py` (строки 654-663)

#### Инкрементальный поиск через last_post_id
- Каждый ресурс в БД хранит `last_post_id` — ID последнего просканированного сообщения
- При следующем сканировании используются только сообщения после `last_post_id`
- **Файл:** `services/scout_parser.py` (строки 485-501, 643-647, 748-753)

#### Интеграция SPY_SKIP_OLD_MESSAGES
- Переменная из `.env` используется только для первого запуска или тестирования
- Если `SPY_SKIP_OLD_MESSAGES > 0`, используется режим пропуска старых сообщений
- Иначе используется инкрементальный режим через `last_post_id`
- **Файл:** `services/scout_parser.py` (строки 485-501, 643-647)

### 2. ✅ AI Analysis (analyzer.py)

#### Обновлены промпты для Яндекс GPT
- Промпт теперь возвращает структурированный JSON с четкими инструкциями
- Добавлена валидация данных (priority_score 1-10, pain_stage ST-1 до ST-4)
- Улучшена обработка ответов от Router AI (очистка от markdown, поиск JSON)
- **Файл:** `services/lead_hunter/analyzer.py` (строки 265-320)

#### Классификация стадий боли
- **ST-1 (Интерес):** Просто интересуется теорией, нет срочности
- **ST-2 (Планирование):** Собирается делать ремонт, ищет варианты
- **ST-3 (Актив):** Уже делает ремонт, боится штрафов, нужен проект
- **ST-4 (Критично):** Получил предписание МЖИ, пришла инспекция, суд, блокировка сделки
- **Файл:** `services/lead_hunter/analyzer.py` (строки 265-285)

#### Priority Score (1-10)
- **1-3:** Низкий приоритет (ST-1)
- **4-5:** Средний приоритет (ST-2)
- **6-7:** Высокий приоритет (ST-3)
- **8-10:** Критический приоритет (ST-4)
- Автоматическая валидация и ограничение в диапазоне 1-10
- **Файл:** `services/lead_hunter/analyzer.py` (строки 292-320)

### 3. ✅ Global Discovery (discovery.py)

#### Реализован global_telegram_search
- Использует Telethon API (`SearchGlobalRequest`) для реального поиска каналов
- Поиск по ключевым словам: "перепланировка", "акт МЖИ", "согласование", "штраф"
- Возвращает только публичные каналы с username
- Ограничение: максимум 10 запросов за раз, 20 результатов на запрос
- **Файл:** `services/lead_hunter/discovery.py` (строки 194-270)

#### Обновлен find_new_sources
- Теперь использует `global_telegram_search` для реального поиска через Telegram API
- Fallback на фильтрацию `OPEN_HUNT_SOURCES` если API поиск не удался
- **Файл:** `services/lead_hunter/discovery.py` (строки 272-291)

#### Реализован scout_vk_resources
- Использует VK API (`groups.search`) для поиска открытых групп ВКонтакте
- Фильтрует только открытые группы (`is_closed == 0`)
- Возвращает список групп для добавления в БД
- **Файл:** `services/lead_hunter/discovery.py` (строки 194-270)

### 4. ✅ Visuals & Publishing

#### Обновлен формат карточки лида
- Добавлены эмодзи для стадий боли:
  - 💡 ST-1 (Интерес)
  - 📋 ST-2 (Планирование)
  - 🔥 ST-3 (Актив)
  - 🚨 ST-4 (Критично)
- Добавлена визуальная шкала приоритета: `████████░░` (10 символов)
- Для ST-4 добавлен специальный заголовок: "🚨 СРОЧНЫЙ ВЫЕЗД/ЗВОНОК"
- **Файл:** `services/lead_hunter/hunter.py` (строки 52-137)

#### Проверка генерации изображений
- RouterAI использует OpenAI format для генерации изображений
- Формат проверен и работает корректно
- **Файл:** `services/image_generator.py` (существующий код)

### 5. ✅ Database (db.py)

#### Добавлена миграция для target_resources
- **priority** (INTEGER DEFAULT 5): Приоритет ресурса от 1 до 10
- **last_scanned_at** (TIMESTAMP NULL): Время последнего сканирования
- Поля `last_post_id`, `type`, `status` уже существовали
- **Файл:** `database/db.py` (строки 260-270)

## Использование

### Запуск LeadHunter
```bash
python3 run_hunter.py
```

### Переменные окружения (.env)
```env
# Инкрементальный поиск (0 = использовать last_post_id, >0 = пропустить первые N сообщений)
SPY_SKIP_OLD_MESSAGES=0

# Ключевые слова для поиска (через запятую, минимум 5 слов)
SCOUT_KEYWORDS=перепланировка,согласование,БТИ,ремонт,Москва

# VK токен для поиска VK групп
VK_TOKEN=your_vk_token
VK_USER_TOKEN=your_vk_user_token  # Альтернатива VK_TOKEN

# Telethon credentials для global_telegram_search
API_ID=your_api_id
API_HASH=your_api_hash
PHONE=your_phone
```

## Результаты

После применения кода:
1. ✅ `python3 run_hunter.py` запускает глобальный поиск через Telegram API
2. ✅ Лиды анализируются с классификацией ST-1 до ST-4 и priority_score 1-10
3. ✅ Карточки лидов отображаются с эмодзи 🚨 для ST-4 и визуальной шкалой приоритета
4. ✅ Инкрементальный поиск использует `last_post_id` для сканирования только новых сообщений
5. ✅ Discovery автоматически находит новые каналы через `global_telegram_search`

## Технические детали

### Инкрементальный поиск
- Каждый ресурс в `target_resources` имеет поле `last_post_id`
- При сканировании используется `min_id=last_post_id` для получения только новых сообщений
- После сканирования `last_post_id` обновляется в БД

### Структурированный JSON от AI
```json
{
    "priority_score": 8,
    "pain_stage": "ST-4",
    "justification": "Клиент получил предписание МЖИ",
    "is_lead": true
}
```

### Визуальная шкала приоритета
```
⭐ Приоритет: 8/10
📊 ████████░░
```

## Файлы изменены

1. `services/lead_hunter/hunter.py` - удалена логика skip_count, обновлен формат карточек
2. `services/lead_hunter/analyzer.py` - обновлены промпты, добавлена валидация JSON
3. `services/lead_hunter/discovery.py` - реализован global_telegram_search и scout_vk_resources
4. `services/scout_parser.py` - добавлен инкрементальный поиск через last_post_id
5. `database/db.py` - добавлена миграция для полей priority и last_scanned_at

## Следующие шаги

1. Протестировать `python3 run_hunter.py` на сервере
2. Проверить работу global_telegram_search (требуется авторизация Telethon)
3. Убедиться, что инкрементальный поиск работает корректно
4. Проверить формат карточек лидов в Telegram
