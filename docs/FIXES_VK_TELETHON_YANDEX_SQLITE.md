# Исправления: VK Discovery, Telethon, Yandex 401, SQLite Timeout

## Дата: 2026-02-17

## Выполненные исправления

### 1. ✅ Исправление VK Discovery (Error 27)

**Проблема:** Метод `scout_vk_resources` использовал `VK_TOKEN` (токен группы) вместо `VK_USER_TOKEN` (токен пользователя) для вызова `groups.search`. VK API Error 27 возникает, когда используется токен группы вместо пользовательского токена.

**Решение:**
- Обновлен метод `scout_vk_resources` для использования `VK_USER_TOKEN` в первую очередь
- Добавлена проверка и предупреждение, если используется `VK_TOKEN` вместо `VK_USER_TOKEN`
- Добавлена обработка VK API Error 27 с четкой инструкцией в логе
- **Файл:** `services/lead_hunter/discovery.py` (строки 319-348)

**Изменения:**
```python
# Было:
vk_token = os.getenv("VK_TOKEN") or os.getenv("VK_USER_TOKEN")

# Стало:
vk_token = os.getenv("VK_USER_TOKEN") or os.getenv("VK_TOKEN")
if os.getenv("VK_USER_TOKEN"):
    logger.debug("✅ Используется VK_USER_TOKEN для поиска VK групп")
elif os.getenv("VK_TOKEN"):
    logger.warning("⚠️ Используется VK_TOKEN вместо VK_USER_TOKEN. groups.search может не работать.")
```

**Обработка Error 27:**
- При обнаружении Error 27 выводится подробная инструкция в лог
- Указано, что требуется `VK_USER_TOKEN` (токен пользователя)
- Прерывается цикл поиска, так как все запросы будут падать с той же ошибкой

### 2. ✅ Фикс авторизации Telethon в Discovery

**Проблема:** Метод `global_telegram_search` создавал новый `TelegramClient` с именем `'discovery_client'`, который не видел существующий файл сессии `'anton_parser.session'` и считал себя неавторизованным.

**Решение:**
- Изменено имя клиента с `'discovery_client'` на `'anton_parser'` (то же, что используется в `scout_parser.py`)
- Это позволяет использовать существующий файл сессии `'anton_parser.session'`
- Улучшено сообщение об ошибке авторизации с указанием на файл сессии
- **Файл:** `services/lead_hunter/discovery.py` (строки 182-188)

**Изменения:**
```python
# Было:
client = TelegramClient('discovery_client', API_ID, API_HASH)

# Стало:
client = TelegramClient('anton_parser', API_ID, API_HASH)
```

**Результат:**
- `global_telegram_search` теперь использует ту же сессию, что и `scout_parser.py`
- Не требуется повторная авторизация
- Улучшена диагностика проблем с авторизацией

### 3. ✅ Обработка Yandex 401

**Проблема:** При ошибке 401 (Unauthorized) от Yandex API не было четкой инструкции для пользователя.

**Решение:**
- Добавлена проверка ответа от `router_ai.generate_response` на наличие информации об ошибке 401
- При обнаружении ошибки 401 выводится подробная инструкция в лог
- Возвращается дефолтный результат вместо падения приложения
- **Файл:** `services/lead_hunter/analyzer.py` (строки 301-325)

**Изменения:**
```python
# Добавлена проверка:
if isinstance(response, str):
    response_upper = response.upper()
    if any(keyword in response_upper for keyword in ["401", "UNAUTHORIZED", "YANDEX_API_KEY", "AUTHENTICATION"]):
        logger.error("=" * 60)
        logger.error("❌ ОШИБКА АВТОРИЗАЦИИ YANDEX API (401 Unauthorized)")
        logger.error("💡 ИНСТРУКЦИЯ: Обновите YANDEX_API_KEY в .env")
        # ... подробные инструкции ...
```

**Результат:**
- При ошибке 401 пользователь получает четкую инструкцию в логе
- Приложение не падает, возвращается дефолтный результат анализа
- Указаны все необходимые шаги для исправления проблемы

### 4. ✅ SQLite Timeout

**Проблема:** При одновременной работе бота и охотника возникала ошибка "database is locked", так как SQLite не ждал освобождения базы.

**Решение:**
- Добавлен параметр `timeout=30.0` в `aiosqlite.connect()`
- Это заставляет ждать освобождения базы до 30 секунд вместо немедленной ошибки
- **Файл:** `database/db.py` (строка 20)

**Изменения:**
```python
# Было:
self.conn = await aiosqlite.connect(self.db_path)

# Стало:
self.conn = await aiosqlite.connect(self.db_path, timeout=30.0)
```

**Результат:**
- При одновременном доступе к БД процессы ждут освобождения базы до 30 секунд
- Устранена ошибка "database is locked" при нормальной работе
- Улучшена стабильность при параллельной работе модулей

## Переменные окружения (.env)

### Для VK Discovery:
```env
# Токен пользователя (обязателен для groups.search)
VK_USER_TOKEN=your_user_token_here

# Токен группы (используется как fallback, но не работает для groups.search)
VK_TOKEN=your_group_token_here
```

### Для Yandex API:
```env
# API ключ Yandex (обязателен)
YANDEX_API_KEY=your_yandex_api_key

# Folder ID (обязателен)
FOLDER_ID=your_folder_id
```

### Для Telethon:
```env
# Убедитесь, что scout_parser.py успешно авторизован
API_ID=your_api_id
API_HASH=your_api_hash
PHONE=your_phone
```

## Тестирование

### VK Discovery:
1. Установите `VK_USER_TOKEN` в `.env`
2. Запустите `python3 run_hunter.py`
3. Проверьте логи на наличие сообщений о поиске VK групп

### Telethon:
1. Убедитесь, что `scout_parser.py` успешно авторизован (файл `anton_parser.session` существует)
2. Запустите `global_telegram_search` через `run_hunter.py`
3. Проверьте, что поиск работает без повторной авторизации

### Yandex 401:
1. Установите неверный `YANDEX_API_KEY` в `.env`
2. Запустите анализ лида
3. Проверьте логи на наличие инструкции об обновлении ключа

### SQLite Timeout:
1. Запустите бота и охотника одновременно
2. Проверьте, что нет ошибок "database is locked"
3. Убедитесь, что процессы корректно ждут освобождения базы

## Файлы изменены

1. `services/lead_hunter/discovery.py` - исправлен VK Discovery и Telethon авторизация
2. `services/lead_hunter/analyzer.py` - добавлена обработка Yandex 401
3. `database/db.py` - добавлен timeout=30 для SQLite

## Следующие шаги

1. Протестировать все исправления на сервере
2. Убедиться, что `VK_USER_TOKEN` установлен в `.env`
3. Проверить, что файл сессии `anton_parser.session` существует
4. Убедиться, что `YANDEX_API_KEY` действителен
