"""
Viral Hooks Agent — генератор цепляющих начал для постов.

Генерирует "хукеры" (hook) — первые строки постов, которые привлекают внимание
и заставляют читать дальше.

Тематика: перепланировки, строительство, недвижимость.
"""
import os
import random
import logging
from typing import List, Dict
from utils import router_ai, yandex_gpt

logger = logging.getLogger(__name__)


# Шаблоны хуков по категориям
HOOK_TEMPLATES = {
    "проблема": [
        "🚨 {проблема} — одна из главных ошибок при перепланировке",
        "⚠️ Почему {проблема} может стоить вам 500 000 ₽",
        "😱 90% собственников не знают про {проблема}",
        "🛑 {проблема} — с этим сталкивается каждый второй",
        "❌ Никогда не делайте {проблема} — вот почему",
        "💥 {проблема} привел к... (реальный случай)",
        "🚫 Забудьте про {проблема} — вот что работает",
        "⏰ Если вы уже сделали {проблема} — читайте срочно",
        "🤔 Думаете {проблема} это нормально? А теперь...",
        "🔴 {проблема}: что нужно знать ДО начала работ",
    ],
    "решение": [
        "✅ Как мы решили {проблема} за 3 дня (реальный кейс)",
        "✨ 5 проверенных способов решить {проблема}",
        "💡 Простое решение {проблема} — работает всегда",
        "🎯 Нашли идеальный способ: {проблема}",
        "🚀 Как {проблема} превратился в {результат}",
        "⭐ Главный секрет: {проблема} — это не страшно",
        "📈 После внедрения {проблема} клиенты сказали...",
        "💎 {проблема} решается в 3 шага — смотрите",
        "🎁 Эксклюзив: {проблема} со скидкой 30%",
        "✅ Наконец-то! {проблема} стало проще простого",
    ],
    "цифры": [
        "📊 {цифра}% людей совершают эту ошибку при {проблема}",
        "💰 {проблема} обошлась в {сумма} ₽ — как избежать",
        "⏱️ {проблема} за {время} — реальный срок",
        "📈 Средняя стоимость {проблема} — {сумма} ₽",
        "🔢 По статистике: {факт} при {проблема}",
        "💯 {проблема}: {цифра} из {цифра} случаев — успешно",
        "📉 Топ-5 ошибок при {проблема}: №{цифра — шокирует",
        "🗓️ За {время} мы сделали {цифра} проектов по {проблема}",
        "📍 {цифра}₽ — именно столько стоит {проблема}",
        "⚡{цифра} дней — именно столько нужно на {проблема}",
    ],
    "истории": [
        "📖 История клиента: «Я хотел {проблема}, но...»",
        "🏠 Как один клиент решил {проблема} и разбогател",
        "👤 Реальный отзыв: «Спасибо за {проблема}!»",
        "🗣️ «Я 5 лет не мог решить {проблема} пока...»",
        "📋 Разбираем реальный случай: {проблема}",
        "🎬 До/После: {проблема} — трансформация за {время}",
        "👥 {цифра} семей уже сделали {проблема} — отзывы",
        "🏆 Лучший проект месяца: {проблема}",
        "🌟 История успеха: от {проблема} к {результат}",
        "🔐 Секретный кейс: как мы скрыли {проблема}",
    ],
    "вопросы": [
        "🤔 Знаете ли вы, что {проблема} — это...?",
        "❓ Что делать, если {проблема} уже случилось?",
        "💭 Хотите узнать, как {проблема} влияет на...?",
        "🧐 Правда ли, что {проблема} — это нормально?",
        "🤨 Почему все молчат про {проблема}?",
        "👀 Замечали {проблема} у соседей? Вот почему...",
        "🔍 Что произойдет, если не решить {проблема}?",
        "🎯 Угадайте: {проблема} — это хорошо или плохо?",
        "🤔 Как вы думаете: {проблема} требует...?",
        "💡 Знаете ли вы, что {проблема} можно...?",
    ],
    "факты": [
        "📢 Малоизвестный факт о {проблема}",
        "🔔 Важное открытие: {проблема} меняет всё",
        "💎 Эксклюзив: {факт} про {проблема}",
        "📢 Сенсация: {проблема} — это не миф",
        "🧠 Интересный факт: {факт} при {проблема}",
        "🔬 Исследование показало: {факт}",
        "📚 Документально подтверждено: {факт}",
        "🎓 Ученые доказали: {факт}",
        "👨‍🔬 Эксперты утверждают: {факт}",
        "🏛️ По закону: {факт} про {проблема}",
    ],
}


class ViralHooksAgent:
    """Агент для генерации вирусных хуков"""
    
    def __init__(self):
        self.router_api_key = os.getenv("ROUTER_AI_KEY") or os.getenv("YANDEX_API_KEY")
        self.folder_id = os.getenv("FOLDER_ID")
        self.use_router = bool(self.router_api_key)
    
    async def generate_hooks(
        self,
        topic: str,
        count: int = 5,
        category: str = None
    ) -> List[Dict]:
        """
        Генерирует хуки по теме.
        
        Args:
            topic: Тема поста (например: "перепланировка квартиры")
            count: Количество вариантов
            category: Категория хуков (или None — все категории)
        
        Returns:
            List[Dict]: Список хуков с категориями
        """
        hooks = []
        
        # Если есть API — используем ИИ
        if self.use_router:
            ai_hooks = await self._generate_with_ai(topic, count, category)
            hooks.extend(ai_hooks)
        
        # Добавляем шаблонные хуки
        template_hooks = self._generate_templates(topic, count, category)
        hooks.extend(template_hooks)
        
        # Перемешиваем и возвращаем уникальные
        random.shuffle(hooks)
        
        # Убираем дубликаты по тексту
        seen = set()
        unique_hooks = []
        for hook in hooks:
            text = hook['text'][:50].lower()
            if text not in seen:
                seen.add(text)
                unique_hooks.append(hook)
        
        return unique_hooks[:count]
    
    async def _generate_with_ai(
        self,
        topic: str,
        count: int,
        category: str = None
    ) -> List[Dict]:
        """Генерирует хуки через ИИ"""
        try:
            system_prompt = """Ты — эксперт по написанию цепляющих заголовков для социальных сетей.
Твоя задача — создавать «хукеры» (hook) — первые строки постов, которые привлекают внимание.

Требования:
- Используй эмоциональные слова: шок, секрет, правда, опасно, важно
- Добавляй конкретику: цифры, суммы, сроки
- Используй формат: проблема → решение
- Будь конкретным, не общими фразами
- Максимум 15 слов на хук

Тема: перепланировки и согласование в Москве.

Формат ответа — просто пронумерованный список, без лишних слов."""

            user_prompt = f"""Создай {count} цепляющих хуков на тему: {topic}

Категории на выбор: проблема, решение, цифры, истории, вопросы, факты
{f'Используй категорию: {category}' if category else 'Используй разные категории'}

Примеры хороших хуков:
- "🚨 Снос несущей стены — одна из главных ошибок при перепланировке"
- "💰 Незаконная перепланировка обошлась в 800 000 ₽ — как избежать"
- "📖 История клиента: «Я хотел объединить кухню, но...»"

Твой ответ — только хуки, по одному на строке, с эмодзи."""

            # Пробуем Router AI
            if self.use_router:
                try:
                    from utils import router_ai
                    response = await router_ai.generate(
                        system_prompt=system_prompt,
                        user_message=user_prompt
                    )
                    if response:
                        return self._parse_ai_response(response, topic)
                except Exception as e:
                    logger.error(f"Router AI error: {e}")
            
            # Fallback на YandexGPT
            try:
                response = await yandex_gpt.generate(
                    system_prompt=system_prompt,
                    user_message=user_prompt,
                    max_tokens=1000
                )
                if response:
                    return self._parse_ai_response(response, topic)
            except Exception as e:
                logger.error(f"YandexGPT error: {e}")
                
        except Exception as e:
            logger.error(f"AI generation error: {e}")
        
        return []
    
    def _parse_ai_response(self, response: str, topic: str) -> List[Dict]:
        """Парсит ответ ИИ в структурированный формат"""
        hooks = []
        lines = response.strip().split('\n')
        
        for line in lines:
            line = line.strip()
            # Убираем нумерацию типа "1." или "1)"
            line = line[line.find('.')+1:].strip() if line[0].isdigit() else line
            line = line[line.find(')')+1:].strip() if ')' in line[:5] else line
            
            if len(line) > 10:
                hooks.append({
                    'text': line,
                    'topic': topic,
                    'source': 'ai'
                })
        
        return hooks
    
    def _generate_templates(
        self,
        topic: str,
        count: int,
        category: str = None
    ) -> List[Dict]:
        """Генерирует хуки из шаблонов"""
        hooks = []
        
        # Определяем проблему из темы
        problem = topic.lower()
        if 'перепланировк' in problem:
            problem = 'перепланировка'
        elif 'ремонт' in problem:
            problem = 'ремонт'
        elif 'согласовани' in problem:
            problem = 'согласование'
        else:
            problem = 'этот вопрос'
        
        # Выбираем категории
        categories = [category] if category else list(HOOK_TEMPLATES.keys())
        
        for cat in categories:
            if cat in HOOK_TEMPLATES:
                templates = HOOK_TEMPLATES[cat]
                for _ in range(min(2, count)):
                    template = random.choice(templates)
                    
                    # Подставляем значения
                    hook = template.format(
                        проблема=problem,
                        результат='уютное жильё',
                        цифра=random.randint(3, 99),
                        сумма=f"{random.randint(1, 9)}00 000",
                        время=f"{random.randint(1, 14)} дней",
                        факт='это важно знать'
                    )
                    
                    hooks.append({
                        'text': hook,
                        'topic': topic,
                        'source': 'template',
                        'category': cat
                    })
        
        return hooks
    
    def generate_hook_for_content(
        self,
        content_type: str,
        topic: str
    ) -> str:
        """
        Генерирует один хук под тип контента.
        
        Args:
            content_type: тип поста (экспертиза, живой, новость, поздравление)
            topic: тема поста
        
        Returns:
            str: Готовый хук
        """
        category_map = {
            'экспертиза': 'факты',
            'живой': 'истории',
            'новость': 'цифры',
            'поздравление': None,  # Пропускаем
        }
        
        category = category_map.get(content_type)
        
        import asyncio
        hooks = asyncio.run(self.generate_hooks(topic, count=3, category=category))
        
        if hooks:
            return hooks[0]['text']
        
        return f"📢 {topic}: важная информация"
    
    def get_categories(self) -> List[str]:
        """Возвращает список доступных категорий"""
        return list(HOOK_TEMPLATES.keys())


# Singleton instance
viral_hooks_agent = ViralHooksAgent()


async def generate_hooks_for_topic(topic: str, count: int = 5) -> List[Dict]:
    """Удобная функция для генерации хуков"""
    return await viral_hooks_agent.generate_hooks(topic, count)


if __name__ == "__main__":
    # Тест агента
    import asyncio
    
    async def test():
        print("🧪 Тест Viral Hooks Agent\n")
        
        topics = [
            "перепланировка квартиры",
            "согласование перепланировки",
            "снос стен в квартире",
            "объединение кухни и гостиной",
        ]
        
        for topic in topics:
            print(f"📝 Тема: {topic}\n")
            hooks = await generate_hooks_for_topic(topic, count=5)
            for i, hook in enumerate(hooks, 1):
                print(f"  {i}. {hook['text']}")
                print(f"     📌 Категория: {hook.get('category', 'ai')}\n")
            print("-" * 50)
    
    asyncio.run(test())
