<<<<<<< HEAD
# БРИФ: Продукт и Работы

## 1. Общее описание проекта

**ТЕРИОН** — система автоматизации поиска и обработки лидов по перепланировкам недвижимости. Состоит из нескольких взаимосвязанных компонентов:

- **ИИ-консультант Антон** — автоматические консультации через Telegram-бота
- **Scout Parser** — мониторинг и парсинг лидов из Telegram и VK
- **LeadHunter** — анализ и генерация ответов на лиды
- **Content Factory** — автоматическая генерация и публикация контента
- **AutoPoster** — автоматическая публикация постов в каналы и MAX.ru
- **Watchdog** — система самовосстановления и мониторинга процессов
- **SessionManager** — управление авторизацией Telethon сессий

---

## 2. Ключевые файлы

### Главный входной файл
- **`main.py`** — основной файл запуска системы. Инициализирует базу данных, запускает планировщик задач (AsyncIOScheduler), управляет двумя ботами (main_bot и content_bot) с раздельными Dispatchers.
- **`watchdog.py`** — система самовосстановления. Автоматически перезапускает main_bot и lead_hunter при падении, записывает ошибки в БД.

### Основные модули

#### База данных
- **`database/db.py`** — работа с SQLite базой данных (parkhomenko_bot.db). Автоматические миграции при старте.

#### Сервисы поиска лидов
- **`services/scout_parser.py`** — парсинг сообщений из Telegram и VK с фильтрацией по ключевым словам и анти-спам проверками
- **`services/lead_hunter/discovery.py`** — глобальный поиск новых чатов/групп через Telegram SearchGlobalRequest
- **`services/lead_hunter/hunter.py`** — анализ лидов и генерация ответов с использованием ИИ, включая еженедельный Insight of the Week

#### Контент и публикация
- **`auto_poster.py`** — автоматическая публикация постов из контент-плана в Telegram-каналы
- **`agents/content_agent.py`** — генерация текстов постов с использованием Yandex GPT
- **`agents/image_agent.py`** — генерация изображений для постов
- **`services/publisher.py`** — публикация в Telegram, VK и MAX.ru с форматированием

#### Авторизация и сессии
- **`session_manager.py`** — управление сессией Telethon для парсинга чатов. Запрашивает код подтверждения при первом запуске.

#### Обработчики бота
- **`handlers/start.py`** — обработка команды /start и квиза
- **`handlers/dialog.py`** — диалоговый режим консультаций
- **`handlers/admin.py`** — административные команды
- **`handlers/content.py`** — управление контентом

#### Конфигурация
- **`config.py`** — загрузка переменных окружения из `.env`
- **`.env.example`** — шаблон переменных окружения

---

## 3. LeadHunter + ScoutParser

### ScoutParser — Мониторинг лидов

**Функционал:**
- Парсинг сообщений из Telegram-групп и VK-групп
- Фильтрация по ключевым словам (`KEYWORDS`, `TECHNICAL_TERMS`)
- Приоритизация по приоритетным ЖК (`PRIORITY_ZHK_NAMES`)
- Инкрементальный парсинг через `last_post_id`
- Гео-тегирование лидов

**Внедрение фильтров "Анти-Спам/Анти-Бот" (6 уровней проверки):**

1. **Исключение каналов** — если сообщение пришло от имени канала (sender_type — канал, а не пользователь), игнорируется
2. **Черный список фраз (реклама)** — список `AD_STOP_WORDS`: "подпишись", "наш канал", "выплата", "канал о недвижимости", "заходи в группу" и т.д.
3. **Детектор рекламных ссылок** — если в одном сообщении более 2-х ссылок или есть ссылка на другой Telegram-канал — в модерацию не отправляется
4. **Фокус на запрос** — остаются только сообщения, где есть вопросительные знаки или слова-маркеры боли (из списка `QUESTION_PATTERNS` и `HOT_TRIGGERS`)
5. **Проверка истории** — если мы уже писали этому пользователю в последние 48 часов, не отправляется повторно
6. **STOP_KEYWORDS** — базовые стоп-слова для исключения нерелевантных тем

### Режим ручной модерации через админку

**При обнаружении лида:**
- Бот формирует расширенную карточку в админ-канал (`LEADS_GROUP_CHAT_ID`, топик `THREAD_ID_HOT_LEADS`)
- В карточке отображаются:
  - **Платформа**: TG/VK
  - **Локация**: Geo Header (например: "ЖК Зиларт, корп. 5")
  - **Приоритет**: Метка "🔥 Высокий", если это приоритетный ЖК
  - **Текст сообщения**: обрезанный до 300 символов
  - **Ссылка на пост**: если доступна

**Интерактивные кнопки:**
- **✅ Одобрить (Антон пишет)** — использует LeadHunter для генерации экспертного ответа, показывает на финальное подтверждение
- **✏️ Редактировать** — ручной ввод ответа
- **🗑 Пропустить** — пропустить лид

**Важно:** Автоматическая отправка ответов лидам **отключена**. Все ответы отправляются только после одобрения через админ-панель.

### LeadHunter — Анализ и генерация ответов

**Функционал:**
- Анализ интента лида через ИИ
- Определение стадии боли (ST-1 до ST-4)
- Генерация персонализированных ответов
- Подбор релевантных кейсов из портфолио
- **Insight of the Week**: еженедельный анализ топовых лидов ST-3/ST-4 для генерации контента в MAX.ru (воскресенье 18:00)
- **Резонанс-фильтр**: автоматическое уведомление админам при постах в VK с 5+ комментариями для потенциального контента

**3-х ступенчатый Fallback для генерации ответов:**

1. **Yandex GPT (основной ключ)** — `YANDEX_API_KEY` из `.env`
2. **Yandex GPT (резервный ключ)** — `YANDEX_API_KEY_BACKUP` из `.env` (если основной не сработал)
3. **Router AI** — `OPENROUTER_API_KEY` из `.env` (если оба ключа Яндекса не сработали)
4. **Статические шаблоны** — последний резерв по стадии боли

**3-х ступенчатый Fallback для анализа интента:**

Аналогичный fallback реализован в методе `_analyze_intent` для обеспечения надежности анализа лидов даже при сбое основного API-ключа.

---

## 4. Агент-Продавец (LeadHunter)

**Текущая реализация:**

- **Анализ интента** — определение намерений клиента через ИИ
- **Определение стадии боли** — классификация по шкале ST-1 (интерес) до ST-4 (критическая ситуация)
- **Генерация ответов** — персонализированные ответы с учетом геолокации и интента
- **Подбор кейсов** — сопоставление с похожими кейсами из портфолио (заглушка, будет реализовано позже)

**3-х ступенчатый Fallback:**
- Yandex GPT (основной ключ) → Yandex GPT (резервный ключ) → Router AI → Статические шаблоны

**Режим модерации:**
- Все ответы проходят через админ-панель перед отправкой
- Кнопки "Одобрить", "Редактировать", "Пропустить" для каждого лида

---

## 5. Content Factory + AutoPoster

### Content Agent

**Функционал:**
- Генерация текстов постов по типам: `news`, `fact`, `case`, `offer`, `seasonal`
- Использование Yandex GPT для генерации
- Асинхронные HTTP-запросы через `aiohttp`

### AutoPoster

**Функционал:**
- Автоматическая публикация постов из таблицы `content_plan` (статус `approved`, `publish_date <= сейчас`)
- Загрузка изображений по `image_url` (поддержка HTTP URL и Telegram file_id)
- HTML-форматирование постов
- Публикация в канал `CONTENT_CHANNEL_ID`
### Publisher (MAX.ru интеграция)

**Функционал:**
- Публикация в Telegram, VK и MAX.ru
- Форматирование постов для MAX.ru с брендингом "Антон, ИИ-ассистент компании TERION"
- Автоматическая генерация инсайтов из лидов ST-3/ST-4
**Интеграция в main.py:**
- Задача планировщика `check_and_publish_scheduled_posts()` запускается по расписанию
- Логирование: "Постов к публикации не найдено" если нет постов для публикации

---

## 6. Discovery — Поиск новых источников

**Функционал:**
- Глобальный поиск новых чатов/групп через Telegram SearchGlobalRequest
- Использование ключевых слов из `ScoutParser` (`KEYWORDS`, `TECHNICAL_TERMS`)
- Команда `/spy_discover` для запуска поиска вручную

**Реализация:**
- Файл `services/lead_hunter/discovery.py`
- Поиск по ключевым словам в глобальном поиске Telegram
- Добавление найденных источников в базу данных (`sources`)

---

## 7. База данных

### Основные таблицы

- **`sources`** — источники для мониторинга (Telegram/VK группы)
  - Поля: `id`, `type`, `source_id`, `name`, `category`, `keywords`, `is_active`, `last_scanned`, `updated_at`
  - Автоматическая миграция при старте: добавление `updated_at` если отсутствует

- **`leads`** — обнаруженные лиды
  - Поля: `id`, `source_id`, `text`, `author_id`, `author_name`, `url`, `published_at`, `status`, `platform`, `geo_tag`, `participants_count`, `is_high_priority`, `last_lead_at`, `contacted_at`

- **`content_plan`** — план публикации контента
  - Поля: `id`, `title`, `body`, `cta`, `status`, `publish_date`, `image_prompt`, `image_url`, `type`, `theme`

- **`unified_leads`** — унифицированные лиды из всех источников

### Миграции

- Автоматические миграции при старте бота (например, добавление `updated_at` в `sources`)
- Файлы миграций в папке `migrations/`

---

## 8. Планировщик задач (main.py)

**Задачи по расписанию:**

1. **ScoutParser.hunt()** — периодический парсинг источников (Telegram/VK)
2. **LeadHunter.hunt()** — анализ новых лидов и отправка карточек в модерацию
3. **LeadHunter.generate_weekly_insight()** — еженедельный анализ топовых лидов ST-3/ST-4 (воскресенье 18:00)
4. **check_and_publish_scheduled_posts()** — публикация постов из контент-плана
5. **competitor_spy.scan_geo_chats()** — мониторинг конкурентных чатов (каждые 5 минут)
6. **creative_agent.scout_topics()** — поиск идей для контента (каждые 6 часов)
7. **Отчеты** — сводки по лидам в 9:00, 14:00, 19:00 (только лиды от "живых людей", фильтр по `author_type`)

**Используется:** `AsyncIOScheduler` из библиотеки `apscheduler`

---

## 9. Конфигурация

### Переменные окружения (.env)

**Telegram:**
- `BOT_TOKEN` — токен основного бота (Антон)
- `CONTENT_BOT_TOKEN` — токен бота для контента (ДОМ ГРАНД)
- `LEADS_GROUP_CHAT_ID` — ID группы для лидов
- `THREAD_ID_HOT_LEADS` — ID топика для горячих лидов
- `THREAD_ID_KVARTIRY`, `THREAD_ID_KOMMERCIA`, `THREAD_ID_DOMA` — ID топиков для разных типов объектов
- `CHANNEL_ID_TERION`, `CHANNEL_ID_DOM_GRAD` — ID каналов для публикации
- `NOTIFICATIONS_CHANNEL_ID` — ID канала для уведомлений

**Yandex Cloud:**
- `YANDEX_API_KEY` — основной API-ключ Yandex GPT
- `YANDEX_API_KEY_BACKUP` — резервный API-ключ Yandex GPT
- `FOLDER_ID` — ID каталога Yandex Cloud

**Router AI:**
- `OPENROUTER_API_KEY` — API-ключ для Router AI (fallback)

**VK:**
- `VK_TOKEN` — токен VK API для парсинга и публикации
- `VK_GROUP_ID` — ID группы VK для мониторинга

**MAX.ru:**
- `MAX_DEVICE_TOKEN` — токен устройства для API MAX.ru
- `MAX_SUBSITE_ID` — ID подсайта в MAX.ru

**База данных:**
- `DATABASE_PATH` — путь к файлу базы данных (по умолчанию `parkhomenko_bot.db`)

**Администратор:**
- `ADMIN_ID` — Telegram User ID администратора

**Telethon:**
- `API_ID`, `API_HASH`, `PHONE` — для авторизации в Telegram API

---

## 10. В планах

### Приоритетные задачи

1. **Подбор кейсов из портфолио** — реализация метода `match_portfolio_cases()` в LeadHunter
2. **Расширение базы знаний** — добавление новых документов в RAG-систему
3. **Улучшение анализа интента** — более точное определение стадии боли
4. **Интеграция с CRM** — автоматическая отправка лидов в CRM-систему
5. **Мониторинг производительности** — добавление метрик и дашбордов для аналитики

### Завершенные задачи (FINAL_RELEASE_V2 — 4 марта 2026)

- ✅ **Архитектурная реорганизация** — слияние кода из патча Жюля, единая БД `parkhomenko_bot.db`, удаление устаревших скриптов (`chat_parser.py`, `vk_parser.py`, `content_agent.py`, `analyzer.py`)
- ✅ **Контент-Хаб MAX.ru** — интеграция `format_max_post`, проверка `CONTENT_BOT_TOKEN`, еженедельный триггер 'Insight of the Week' для лидов ST-3/ST-4
- ✅ **Резонанс-фильтр** — уведомления админам при постах в VK с 5+ комментариями
- ✅ **Стабильность и безопасность** — `watchdog.py` как главный процесс контроля Антона и Шпиона, новая логика `SessionManager`
- ✅ **Брендинг** — унификация подписи "Антон, ИИ-ассистент компании TERION" во всех публикациях
- ✅ **VK-парсинг** — реализован в `services/scout_parser.py`
- ✅ **Фильтрация лидов** — реализованы 6 уровней анти-спам фильтров
- ✅ **Режим модерации** — реализована ручная модерация через админ-панель
- ✅ **3-х ступенчатый Fallback** — реализован в LeadHunter для генерации ответов и анализа интента
- ✅ **Discovery** — реализован глобальный поиск новых источников
- ✅ **Автоматическая публикация контента** — реализована в `auto_poster.py` с загрузкой изображений

---

## 11. Запуск системы

### Через Watchdog (рекомендуемый способ)

```bash
python watchdog.py
```

Watchdog автоматически запускает и мониторит процессы `main_bot` и `lead_hunter`, перезапуская их при падении.

### Через PM2

```bash
pm2 restart anton-2-bot
```

### Прямой запуск (для отладки)

```bash
python main.py  # Запуск обоих ботов
python run_anton.py  # Только Антон
python run_hunter.py  # Только LeadHunter
```

**Важно:** При запуске автоматически выполняются миграции базы данных.

---

## 12. Архитектура

### Два бота с раздельными Dispatchers

1. **main_bot (АНТОН)** — консультант по перепланировкам
   - Обработчики: `/start`, квиз, диалог, админ-команды
   - Интеграция с LeadHunter и ScoutParser

2. **content_bot (ДОМ ГРАНД)** — контент и посты
   - Обработчики: управление контентом, публикация

### Единый источник истины

- Bot и Dispatcher создаются только в `main.py`
- Остальные модули получают бота через `utils.bot_config.get_main_bot()` или из контекста (`message.bot`, `callback.bot`)

### Неубивайка

- Lock-файл `bot.lock` для предотвращения запуска нескольких экземпляров
- Один процесс на инстанс

---

## 14. Watchdog — Система самовосстановления

**Функционал:**
- Автоматический запуск и мониторинг процессов `main_bot` и `lead_hunter`
- Перезапуск процессов при падении с экспоненциальной задержкой
- Запись ошибок в базу данных
- Graceful shutdown при получении сигналов SIGINT/SIGTERM

**Файл:** `watchdog.py`

**Процессы под контролем:**
- `main_bot`: `python run_anton.py`
- `lead_hunter`: `python run_hunter.py`

---

## 15. SessionManager — Управление авторизацией

**Функционал:**
- Создание и управление сессией Telethon для парсинга чатов
- Запрос кода подтверждения при первом запуске
- Валидация номера телефона и API credentials

**Файл:** `session_manager.py`

**Переменные:**
- `API_ID`, `API_HASH`, `PHONE` — из `.env`
- Сессия сохраняется в `anton_parser.session`

---

## 14. Безопасность

### Защита от спама и ботов

- 6 уровней фильтрации в ScoutParser
- Проверка типа отправителя (только пользователи, не каналы)
- Черный список рекламных фраз
- Детекция избыточных ссылок
- Проверка истории контактов (48 часов)

### Режим модерации

- Все ответы лидам проходят через админ-панель
- Автоматическая отправка ответов отключена
- Интерактивные кнопки для одобрения/редактирования/пропуска

---

## 15. Мониторинг и отчеты

**Автоматические отчеты:**
- 9:00, 14:00, 19:00 — сводки по лидам за последние 24 часа
- Фильтр "Живой человек" — только лиды от реальных пользователей (не ботов, не каналов)

**Команды администратора:**
- `/spy_discover` — запуск поиска новых источников
- `/spy_report` — отчет по последнему скану ScoutParser
- Другие административные команды в `handlers/admin.py`

---

## 16. Логирование

**Уровни логирования:**
- `INFO` — основные события (запуск задач, обнаружение лидов, публикация постов)
- `WARNING` — предупреждения (fallback на резервный API, ошибки загрузки изображений)
- `ERROR` — критические ошибки (записываются в БД через DbLogHandler)
- `DEBUG` — детальная отладочная информация

**Формат:** `%(asctime)s - %(name)s - %(levelname)s - %(message)s`

**Особенности:**
- Ошибки уровня ERROR автоматически записываются в таблицу `system_logs` БД
- Watchdog логирует запуски и падения процессов
- Все логи сохраняются в `watchdog.log` для аудита

---

- Все изменения в коде синхронизированы с PM2 через `pm2 restart anton-2-bot`
- База данных автоматически мигрируется при старте бота
- Конфигурация загружается из `.env` через `config.py`
- Все асинхронные операции используют `asyncio` и `aiogram 3.x`
=======
# Бриф: продукт, что сделано и что в планах

**Обновлено:** 17 февраля 2026 (последнее обновление: LeadHunter v3.1, режим модерации, фильтры анти-спама (6 уровней), 3-х ступенчатый Fallback Агента-Продавца, фильтр "Живой человек" для сводок)  
**Назначение:** единый ориентир для разработки и для ИИ-агента (Cursor): по нему сверять действия и вносить изменения. При внесении изменений обновляйте разделы 4–5 и при необходимости 2.

---

## 1. Продукт и цель

- **Что:** экосистема ТЕРИОН — Telegram-боты и контент для эксперта по перепланировкам **Юлии Пархоменко** (компания ТЕРИОН).
- **Для кого:** собственники и инвесторы (Москва и МО, другие регионы по возможностям).
- **Задачи:**
  - Консультации по перепланировкам (ИИ + база знаний, без обещаний «узаконить что угодно»).
  - Сбор заявок через квиз и мягкий подвод к заявке в диалоге.
  - Контент для каналов (посты, прогревы, кейсы) и публикация в TG и VK.
  - Поиск лидов (ScoutParser, LeadHunter) и **Умный Охотник v2.0 (TERION)**: в реальном времени карточки лидов в рабочую группу при hotness ≥ 3 (Агент-Шпион), экспертные ответы и «Взять в работу» (Агент-Антон); интеграция квиза с предварительным заключением и уведомлением Юлии.

---

## 2. Текущая архитектура

| Компонент | Описание |
|-----------|----------|
| **Main Bot (АНТОН)** | Консультант, квиз, диалог с RAG. Токен: `BOT_TOKEN`. |
| **Content Bot (ДОМ ГРАНД)** | Контент-меню, генерация постов, публикация в каналы и VK. Токен: `CONTENT_BOT_TOKEN`. |
| **Запуск** | Один процесс `main.py` — два Dispatcher’а (main_bot + content_bot), один PM2-процесс `anton-2-bot`. |
| **Сервер** | 176.124.219.183, проект `~/PARKHOMENKO_BOT`, Python + PM2 (без Docker). |
| **БД** | SQLite (`database/bot.db`), контент-посты в той же БД. Лиды hunter_standalone: `database/potential_leads.db`. |
| **База знаний** | `knowledge_base/` (80+ документов), RAG через `utils/kb.py`. |

Ключевые файлы:
- `main.py` — точка входа, два бота, scheduler (**LeadHunter каждые 30 мин**, Creative Agent раз в 6 ч, **сводки лидов: 9:00/14:00/19:00 МСК**, проверка горячих лидов каждые 15 мин). Интегрирован обновленный ScoutParser с фильтрами анти-спама и режимом модерации.
- `run_anton.py` — главный файл запуска системы TERION (альтернативный запуск): бесконечный цикл сканирования с фильтрами анти-спама, режим модерации, поддержка резервного API-ключа Яндекса.
- `handlers/` — start, quiz, dialog, content, admin.
- `handlers/content.py` — всё контент-меню: фото→пост, ИИ-визуал, 7 дней прогрева, новость, план, быстрый текст; публикация в TG/VK; подпись «Эксперт: Юлия Пархоменко».
- `services/lead_hunter/` — парсинг TG/VK (ScoutParser), анализ (LeadAnalyzer), outreach; интеграция с `hunter_standalone`; карточки лидов на модерацию с кнопками «Одобрить/Редактировать/Пропустить»; **фильтры анти-спама (6 уровней проверки)**; **режим ручной модерации через админку**.
- `services/scout_parser.py` — парсинг TG/VK с **фильтрами анти-спама**: исключение каналов, черный список рекламных фраз, детектор ссылок, фокус на запрос, проверка истории контактов.
- `hunter_standalone/` — БД потенциальных лидов, оценка hotness через Агента-Шпиона (Yandex AI Agent API или fallback Router AI).
- `utils/yandex_ai_agents.py` — вызов Агента-Шпиона (ID fvt2vnpq2qjdt829em50) и Агента-Антона (fvtrdfvmv1u84s9rfn5a): анализ сообщений, экспертный ответ до 500 знаков с дисклеймером и призывом квиза, предварительное заключение по сводке квиза.
- `config.py` — все переменные из `.env` (BOT_TOKEN, VK_TOKEN, ADMIN_ID, YANDEX_AI_AGENT_SPY_ID, YANDEX_AI_AGENT_ANTON_ID, JULIA_CONTACT, топики, каналы и т.д.).

---

## 3. Деплой и команды

- **Сервер:** `root@176.124.219.183`, проект: `~/PARKHOMENKO_BOT`.
- **Обновление кода и рестарт:**  
  `git pull origin main && pm2 restart anton-2-bot`
- **Проверка статуса и логов:**  
  `pm2 list && pm2 logs anton-2-bot --lines 30 --nostream`
- В Cursor по фразам «проверь снова» / «запусти деплой» агент выполняет эти действия на сервере (см. правило в `.cursor/rules/deploy-server.mdc`).

---

## 4. Что сделано (актуальное состояние)

### Инфраструктура
- Два бота в одном процессе (АНТОН + контент), PM2 на 176.124.219.183.
- Квиз (сбор заявки), диалог с RAG, короткие ответы без повторов, учёт контекста.
- **AI:** Router AI (`ROUTER_AI_KEY`) — чат и изображения; Yandex GPT — анализ лидов, генерация ответов продавца; опционально Яндекс АРТ для картинок.
- **Генерация изображений:** без текста на изображении (no text, no words, no letters) — промпты с явным запретом.
- Контент: генерация постов, «7 дней прогрева», публикация в каналы ТЕРИОН, ДОМ ГРАНД, VK, MAX. Кнопки в черновиках в рабочей группе.

### Умный Охотник v3.1 (LeadHunter — февраль 2026)

- **AsyncIOScheduler: каждые 20 минут** — шпион ищет лиды каждые 20 мин через `run_hunter.py`.
- **WAL режим БД:** SQLite работает в режиме Write-Ahead Logging для предотвращения ошибок "database is locked" при параллельных запросах. Добавлены `PRAGMA busy_timeout=5000` и `PRAGMA synchronous=NORMAL` для улучшения concurrency.
- **Планировщик отчётов:** итоговые отчёты отправляются строго в 9:00, 14:00, 19:00 МСК через `CronTrigger`.
- **Метод `send_daily_report()`:** сводка за 24 часа (всего просмотрено сообщений, найдено лидов, горячие/обычные) в топик "Логи".
- **`last_post_id` в ScoutParser:** каждый чат запоминает ID последнего обработанного сообщения; повторная обработка исключена; прежнее дефолтное «пропустить 78» удалено.
- **Стадии боли (ST-1…ST-4):** `LeadAnalyzer` и `HunterDatabase` хранят `pain_stage` и `priority_score` для каждого лида; миграции БД добавлены в `db.py` и `hunter_standalone/database.py`.
- **Увеличение глубины сканирования:**
  - Глобальный лимит `SCAN_LIMIT = 100` сообщений (вместо 50) в `config.py`.
  - Telegram: лимит увеличен до 100 сообщений за цикл.
  - VK: `wall.get` использует до 100 постов (лимит VK API), адаптивные параметры для комментариев.
  - Умные задержки: `await asyncio.sleep(0.5)` каждые 20 сообщений для предотвращения FloodWait от Telegram API.
  - Задержки добавлены в циклы парсинга Telegram и VK (посты и комментарии).
- **VK парсинг из БД `target_resources`:**
  - Приоритетная загрузка VK групп из БД (`resource_type='vk'`, `is_active=1`) вместо хардкода.
  - Парсинг всех 157 групп из БД автоматически каждые 20 минут.
  - Безопасная обработка ошибок: приватные/забаненные группы пропускаются без остановки цикла.
  - Логирование: `"VK Scan: Processing [Group Name] ([ID]) - [N] new posts found"` для каждой группы.
  - Fallback на `.env` если БД пуста (обратная совместимость).
- **Lead Visualizer и AI Scoring refinement:**
  - Приоритетные ключевые слова дают boost к `priority_score`: "перепланировк" (+2), "узакони" (+2), "бти" (+2), "проект проема" (+2).
  - Boost применяется в `TRIGGER_WORDS` и после AI-анализа, максимум score = 10.
  - Обновлённый формат Lead Card: статус (эмодзи), источник, контент (300 символов), user handle, визуальная шкала приоритета.
  - Кнопки действий: "🔗 Перейти к сообщению" (URL), "✅ В работу" (обновляет статус в БД), "🛠 Ответить экспертно".
  - Метод `mark_lead_in_work()` в БД для обновления статуса лида.
- **Тихие уведомления для низкоприоритетных лидов:**
  - Если `priority_score < 8` → `disable_notification=True` (тихое уведомление без звука).
  - Если `priority_score >= 8` → `disable_notification=False` (обычное уведомление).
  - Применяется в `_send_lead_card_to_group()` и `send_hot_leads_immediate()`.
- **Итоговое логирование цикла:**
  - В конце каждого цикла `hunt()` логируется: `"Cycle complete: [X] TG messages scanned, [Y] VK posts scanned, [Z] Hot leads found"`.
  - Подсчёт горячих лидов из БД за последний час (priority_score >= 8 или ST-3/ST-4).
- **Стратегия «Открытая охота»:**
  - Шпион не пытается войти в закрытые ЖК-чаты.
  - Мониторится 18 крупных открытых каналов: недвижимость, ремонт, дизайн интерьеров, жители Москвы (`scripts/init_spy_targets.py`).
  - Группа **ТЕРИОН / перепланировки** исключена из сканирования VK — ресурсы только на внешние источники.
- **Детектор приоритетных ЖК (`_detect_priority_zhk_hot`):**
  - Если в любом канале упомянуты Зиларт / Символ / Сердце Столицы / Династия / Пресня Сити **+** слова суд / штраф / МЖИ / предписание / перепланировка / объединение и др. → немедленно **ST-4**, score 9, без ожидания ИИ.
  - Принудительное повышение до ST-4 даже если Yandex AI занизил оценку.
  - Тройная страховка: до ИИ / после ИИ / в fallback при сбое.
- **Discovery (`services/lead_hunter/discovery.py`):** `DEFAULT_KEYWORDS` включает названия ЖК («Зиларт», «Династия», «Символ», «Сердце Столицы»); `OPEN_HUNT_SOURCES` — пул из 14 открытых каналов; фильтрация по ключевым словам и приоритетным ЖК.
- **Агент-Продавец (`_generate_sales_reply`):**
  - Читает `knowledge_base/sales/hunter_manual.md` (роль «Друг-эксперт», оффер TERION).
  - Вызывает Yandex GPT с ролью «Агент-Продавец».
  - Генерирует живой ответ 2–4 предложения под стадию боли (ST-1…ST-4).
  - Fallback-шаблоны при сбое ИИ.
- **Карточка Юлии (новый формат):**
  - Заголовок ЖК (🚨 ГОРЯЧИЙ ЛИД — ЖК Зиларт или 🔥 Новый лид).
  - Стадия боли с эмодзи (⛔ Критично / 🔴 Активная боль / 🟡 Планирование / 🟢 Интерес).
  - Ссылка на автора: `tg://user?id=...` (Telegram) или `vk.com/idXXX` (VK).
  - Блок `<code>` с проектом ответа — тап → копировать.
  - Инлайн-кнопки: **[👤 Написать автору]** и **[🔗 Открыть пост]**.
- **Улучшенный лог ошибок парсера:** три разных уровня — несуществующий username / приватная invite-ссылка / общая ошибка — с инструкцией по действию в каждом случае.
- **Планировщик сводок лидов (17.02.2026):**
  - Отправка сводок обычных лидов (priority < 3) трижды в день: **9:00, 14:00, 19:00 МСК** в топик "Логи" через `send_regular_leads_summary()`.
  - Немедленная отправка горячих лидов (priority ≥ 3, HOT_TRIGGERS) в топик "Горячие лиды" (THREAD_ID_HOT_LEADS).
  - Проверка неотправленных горячих лидов каждые 15 минут.
  - Различение типов лидов: горячие → немедленно в топик THREAD_ID_HOT_LEADS, обычные → сводка по расписанию в топик THREAD_ID_LOGS.
  - Методы: `send_regular_leads_summary()`, `send_hot_leads_immediate()` в `hunter.py`.
  - БД: колонка `sent_to_hot_leads` в `spy_leads`, методы `get_regular_leads_for_summary()`, `get_hot_leads_for_immediate_send()`, `mark_lead_sent_to_hot_leads()`.
  - **Фильтр "Живой человек":** в методах БД `get_regular_leads_for_summary()` и `get_hot_leads_for_immediate_send()` добавлена проверка `author_id IS NOT NULL AND author_id != '' AND author_id != '0'` для исключения лидов от каналов. Только сообщения от реальных пользователей попадают в сводки 9:00/14:00/19:00 и в топик "Горячие лиды".
- **Улучшенная логика детекции лидов:**
  - Расширенные списки ключевых слов: KEYWORDS, TECHNICAL_TERMS, COMMERCIAL_MARKERS, QUESTION_PATTERNS в `ScoutParser`.
  - Реалистичная оценка priority_score (0-5): 0-1 — общий интерес, 2 — умеренный запрос, 3 — явный запрос услуги, 4-5 — горячий запрос.
  - HOT_TRIGGERS: критичные фразы для немедленного определения горячего лида.
  - Статистика сканирования: `total_scanned`, `total_with_keywords`, `total_leads`, `total_hot_leads` в `ScoutParser`.
  - **Фильтрация лидов через анти-спам фильтры:** **СДЕЛАНО** (удалено из "В планах").
- **Режим отладки:** `SCOUT_DEBUG=True` в `.env` для логирования первых N сообщений с причинами фильтрации.
- **Исправление async загрузки VK групп:** `_load_vk_groups()` стал async, инициализация `self.vk_groups = []` в `__init__`, реальная загрузка из БД происходит в `parse_vk(db=db)`.

### Квиз и диалог
- После завершения квиза сводка → Агент-Антон → «Предварительное заключение» клиенту; уведомление в рабочую группу.

### Content Engine (Creative Agent + AutoPoster)

- **Creative Agent (`agents/creative_agent.py`):**
  - Генерация идей контента по расписанию (раз в 6 часов).
  - Адаптивный STYLE_PRESET: изображения соответствуют теме поста (массовое жильё, технические темы, новости).
  - Режим новостей: детекция по ключевым словам (новости, ипотека, закон, МЖИ), запрет на выдумывание цифр и условий.
  - Оптимизация генерации: `max_tokens=800` для исключения технических обрывов текста.
  - Требования к тексту: оптимальный объем 600-1000 знаков, логическое завершение, короткие абзацы.
  - Обязательные элементы: ссылка на квиз (`VK_QUIZ_LINK`) и хэштеги (#перепланировка #МЖИ #БТИ #TERION) в системном промпте.

- **AutoPoster (`auto_poster.py`):**
  - Публикация только одного самого старого поста за цикл (каждые 10 минут).
  - Обязательная проверка изображения: если `image_url` пустой или некорректен, пост не публикуется и ждёт генерации.
  - Финальный контроль оформления: проверка наличия ссылки на квиз и обязательных хэштегов (#TERION #перепланировка) перед публикацией.
  - Автоматическое добавление недостающих элементов (футер с квизом и хэштеги) если их нет в тексте от ИИ.
  - Определение канала публикации: TERION или ДОМ ГРАНД на основе ключевых слов в теме/заголовке/теле поста.

- **Скрипт исправления контент-плана (`fix_content.py`):**
  - Сброс опубликованных постов без изображений за последние 5 часов.
  - Возврат постов в статус `approved` для повторной обработки AutoPoster с правильными интервалами.

### Data-Driven Scout
- Цели в БД (`target_resources`, `status='active'`). Команды: `/approve_targets`, `/add_target`, `/scan_chats`, `/spy_discover`. Geo-теги в заголовках карточек лидов.

---

## 5. Дорожная карта и очередь задач

### ✅ Сделано (февраль 2026)

**LeadHunter v3.1:**
- ✅ Шпион раз в 20 мин через `run_hunter.py` с AsyncIOScheduler.
- ✅ WAL режим БД для предотвращения ошибок "database is locked" (`PRAGMA busy_timeout=5000`, `PRAGMA synchronous=NORMAL`).
- ✅ Планировщик отчётов: строго 9:00, 14:00, 19:00 МСК через `CronTrigger`.
- ✅ Метод `send_daily_report()`: сводка за 24 часа в топик "Логи".
- ✅ `last_post_id`, стадии боли ST-1…ST-4, pain_stage / priority_score в БД.
- ✅ Приоритетные ЖК: детектор, форсированный ST-4, расширенный список источников (18 каналов).
- ✅ Агент-Продавец: Yandex GPT + База Знаний TERION → проект ответа под стадию боли.
- ✅ Карточка Юлии: заголовок ЖК, стадия с эмодзи, ссылка на автора, блок `<code>`, кнопки «Написать автору» / «Открыть пост».
- ✅ Улучшенные логи парсера (приватная / битая / общая ошибка).
- ✅ Удалена группа TERION из VK-сканирования.
- ✅ Discovery: ЖК-имена в DEFAULT_KEYWORDS, расширен пул каналов.
- ✅ Расширенные списки ключевых слов для реалистичной детекции лидов.
- ✅ Режим отладки: `SCOUT_DEBUG=True` для логирования фильтрации сообщений.
- ✅ **Увеличение глубины сканирования:** `SCAN_LIMIT = 100` сообщений, умные задержки каждые 20 сообщений для предотвращения FloodWait.
- ✅ **VK парсинг из БД:** автоматическая загрузка всех 157 групп из `target_resources`, безопасная обработка ошибок, логирование прогресса. **СДЕЛАНО** (удалено из "В планах").
- ✅ **Lead Visualizer:** обновлённый формат карточек с визуальной шкалой приоритета, boost для ключевых слов, кнопка "В работу" с обновлением статуса в БД.
- ✅ **Тихие уведомления:** `disable_notification=True` для лидов с `priority_score < 8`, обычные уведомления для критических лидов (`>= 8`).
- ✅ **Итоговое логирование:** `"Cycle complete: [X] TG messages scanned, [Y] VK posts scanned, [Z] Hot leads found"` в конце каждого цикла.
- ✅ **Режим модерации (февраль 2026):** все лиды отправляются в админ-канал (топик THREAD_ID_HOT_LEADS) для ручной модерации. Карточки содержат платформу (TG/VK), локацию (Geo Header), приоритет (🔥 Высокий для приоритетных ЖК), интерактивные кнопки: «✅ Одобрить (Антон пишет ответ)», «✏️ Редактировать», «🗑 Пропустить». При одобрении генерируется ответ через Router AI с показом на финальное подтверждение.
- ✅ **Фильтры анти-спама (6 уровней проверки):** исключение каналов (sender_type == "channel"), черный список рекламных фраз (AD_STOP_WORDS), детектор ссылок (>2 ссылок или ссылка на другой Telegram-канал), фокус на запрос (вопросы или маркеры боли), проверка истории контактов (уже писали в последние 48 часов), фильтр "Живой человек" (только сообщения от пользователей, не от каналов).
- ✅ **3-х ступенчатый Fallback Агента-Продавца:** Yandex API ключ 1 → Yandex API ключ 2 (YANDEX_API_KEY_BACKUP) → Router AI → статические шаблоны по стадии боли. Поддержка резервного ключа Яндекса в `utils/yandex_gpt.py`.
- ✅ **Синхронизация с новыми полями БД:** использование `platform` и `is_high_priority` в логике генерации ответов. Антон понимает контекст: отвечает в приоритетный ЖК или обычную группу, адаптирует тон под платформу (VK/Telegram).

**Content Engine:**
- ✅ Creative Agent: адаптивный STYLE_PRESET, режим новостей без выдумывания фактов.
- ✅ Оптимизация генерации: `max_tokens=800`, требования к объёму (600-1000 знаков), логическое завершение текста.
- ✅ AutoPoster: публикация одного поста за цикл, обязательная проверка изображений.
- ✅ Финальный контроль оформления: автоматическое добавление ссылки на квиз и хэштегов (#TERION #перепланировка).
- ✅ Скрипт `fix_content.py` для сброса опубликованных постов без изображений.

### 🔧 Ближайшие задачи (очередь)

1. **Проверить LeadHunter v3.1 на сервере** — после `git pull && pm2 restart anton-2-bot`:
   - Убедиться, что LeadHunter запущен через планировщик в `main.py` (каждые 30 минут).
   - Проверить работу сводок в 9:00, 14:00, 19:00 МСК через `send_regular_leads_summary()`.
   - Проверить фильтр "Живой человек": в сводки попадают только лиды с `author_id` (не от каналов).
   - Проверить отправку горячих лидов каждые 15 минут.
   - Убедиться, что БД работает в WAL режиме (логи: "База данных подключена (WAL режим)").
   - Проверить статистику сканирования: `total_scanned`, `total_with_keywords`, `total_leads` в логах.
   - Проверить VK парсинг: логи `"VK Scan: Processing [Group Name] ([ID]) - [N] new posts found"`.
   - Проверить итоговое логирование: `"Cycle complete: [X] TG messages scanned, [Y] VK posts scanned, [Z] Hot leads found"`.
   - Проверить тихие уведомления: лиды с `priority_score < 8` должны приходить без звука.

2. **Проверить шпиона на сервере:**
   - Убедиться, что `database/` содержит `bot.db` и `potential_leads.db`.
   - Запустить `python scripts/init_spy_targets.py` — загрузит 18 целей в БД.
   - Посмотреть логи: `pm2 logs anton-2-bot --lines 50` — нет ли ошибок авторизации Telegram-клиента.
   - Проверить, что уведомления Юлии доходят (JULIA_USER_ID в `.env`).

3. **Верифицировать usernames каналов** — часть из 18 каналов (`zilart_msk`, `zhk_simvol_msk` и др.) могут иметь несовпадающие usernames. При ошибке «НЕСУЩЕСТВУЮЩИЙ USERNAME» в логе — заменить на корректный или числовой `chat_id`.

4. **VK:** проверить `VK_TOKEN` на сервере, при ошибке `invalid access_token` — обновить токен.

5. **Контент:** расписание публикаций, форматы превью, кнопки TERION / ДОМ ГРАНД / MAX / VK из рабочей группы.

6. **Проверить Content Engine на сервере:**
   - Убедиться, что Creative Agent генерирует посты с обязательными элементами (квиз, хэштеги).
   - Проверить работу AutoPoster: публикация одного поста за цикл, проверка изображений.
   - При необходимости запустить `fix_content.py` для сброса постов без изображений.

### 🚧 В работе (текущий этап)

1. **Модуль «ИИ-Продавец» (Sales Agent) — частично реализовано:**
   - ✅ Генерация проекта ответа через `_generate_sales_reply` (Yandex GPT + База Знаний).
   - ✅ Проект ответа в карточке Юлии (блок `<code>` для копирования).
   - ✅ **5-шаговый скрипт продаж реализован** в `handlers/sales_agent.py`:
     - ✅ Шаг 1: Приветствие + Квалификация (квартира/коммерция, район, что менять).
     - ✅ Шаг 2: Продажа ценности (бесплатный разбор плана на риски).
     - ✅ Шаг 3: Захват документа (получение фото БТИ или эскиза).
     - ✅ Шаг 4: Презентация УТП (прозрачность через бота 24/7, оплата по факту).
     - ✅ Шаг 5: Переход к квизу.
   - ✅ State-Machine (StatesGroup) реализован в `SalesStates`.
   - ✅ Методы БД для продажных диалогов (`save_sales_conversation`, `get_sales_conversation`, `update_sales_conversation`).
   - ✅ Пометка ИИ: "Я Антон, ИИ-помощник эксперта Юлии Пархоменко" в шаблоне приветствия.
   - ✅ Автоматические напоминания (24ч и 3 дня) реализованы в `services/sales_reminders.py`, запускаются каждые 6 часов через планировщик.
   - ⏳ **В процессе:** Интеграция в основной диалог (`handlers/dialog.py`) — автоматический запуск `start_sales_flow` при обнаружении лида в комментариях или при первом контакте.

2. **Парсинг комментариев в чатах (Discussion Groups) — в планах:**
   - ⏳ Реализация `get_discussion_message` в `scout_parser.py` для автоматического перехода из канала в связанный чат.
   - ⏳ Парсинг веток комментариев под постами в поисках фраз: «а как узаконить?», «кто делал проект?», «какие документы нужны?».
   - ⏳ Мгновенный ответ Антона в комментариях (публичный ответ в ветке обсуждения).

3. **Расширение Discovery Engine — в планах:**
   - ⏳ Расширенный поисковый запрос для поиска сотен чатов (не только 3).
   - ⏳ Комбинация ключевых слов с названиями районов (ЮВАО, ЗАО и т.д.).
   - ⏳ Глобальный поиск Telegram через `SearchGlobalRequest` (уже частично реализовано в `global_telegram_search`).

### 📋 В планах (следующий этап)

- **Опрос для экспертной оценки** — квиз-сценарий: данные об объекте → экспертная оценка → консультация.
- **Калькулятор по перепланировке** — оценка объекта с узаконенной перепланировкой / без, риски, снижение стоимости при продаже. Расширить `handlers/invest.py` или добавить отдельный блок.
- **Яндекс Дзен** — дистрибуция контента.
- **Mini App в Telegram** — квиз и калькулятор внутри приложения.
- **Лендинг** — отдельный ресурс.
- **Сценарии оплат** — интеграция платёжного шлюза.

**Правила постов (действующие):**
- Хэштеги обязательны: `CONTENT_HASHTAGS` (по умолчанию: #TERION #перепланировка #недвижимость #москва). Переопределяется в `.env`.
- Ссылка на квиз — в каждый пост (`ensure_quiz_and_hashtags` в `handlers/content.py`).

**Правила генерации контента (Creative Agent + AutoPoster):**

1. **Объём и завершённость текста:**
   - `max_tokens=800` для исключения технических обрывов текста.
   - Оптимальный объём: 600-1000 знаков.
   - ЗАПРЕЩЕНО обрывать текст на полуслове. Пост должен быть логически завершён.
   - Если лимит символов исчерпан, сокращай вводную часть, но не финал.
   - Используй короткие абзацы для лучшей читаемости.

2. **Режим "Новости без фантазии":**
   - Детекция новостных тем по ключевым словам: новости, ипотека, закон, МЖИ.
   - ЗАПРЕЩЕНО выдумывать цифры и условия (например, про ПВ или ставки).
   - Если точных данных в Базе Знаний нет, пиши только подтверждённую информацию о событии без домыслов.
   - Стиль изображений для новостей: "Realistic cityscape of Moscow or official document style. No interior renders."

3. **Гарантия оформления (AutoPoster):**
   - Бот ОБЯЗАН проверить наличие ссылки на квиз (`VK_QUIZ_LINK`) и обязательных хэштегов (#TERION #перепланировка) перед публикацией.
   - Если их нет в тексте от ИИ — добавить принудительно в футер перед публикацией.
   - Логирование добавленных элементов для отладки.

4. **Исправление очереди публикации:**
   - За один цикл (`check_and_publish`) публикуется только один самый старый пост.
   - Если `image_url` отсутствует или некорректен, пост не публикуется и ждёт генерации изображения.
   - Проверка успешности скачивания изображения (HTTP 200, непустое содержимое).

---

## 6. Роли и правила для доработок

- **Юлия Пархоменко** — владелец продукта, эксперт, финальные решения по поведению и контенту.
- **ИИ-агент (Cursor):** вносит изменения по ТЗ; перед правками — план (что меняем), затем правки; не менять онбординг, квиз и логику лидов без явного запроса; секреты только в `.env`, не коммитить токены.

---

## 7. Юлия как эксперт (для консультанта и контента)

- **Кто:** «Юлия Пархоменко — эксперт по перепланировкам и согласованию недвижимости, помогаю собственникам квартир и инвесторам в Москве и области безопасно согласовать перепланировки и избежать штрафов.»
- **Объекты:** панельные/монолитные дома, кирпичный старый фонд, апартаменты, коммерция на 1–2 этажах МКД.
- **География:** реальные кейсы — Москва и МО, СПб, Краснодарский край, Екатеринбург; по остальным регионам — только общие ориентиры.
- **Не работаем:** грубые нарушения (несущие, мокрые зоны над жильём), уже идущие суды с соседями, самострой без документов.
- **Тон:** спокойный, человечный, без канцелярита; риски обозначаем честно, без запугивания; не использовать «обойти закон», «узаконить любой ценой».

---

## 8. Контент-агент и канал (план и факт)

**План по контенту:** публикации по теме проекта, новости, интересные факты, поздравления с официальными праздниками РФ, поздравления с ДР в личку подписчикам, актуальные темы от креативщика, загрузка фото → ИИ описание → согласование → публикация (фото сжимается под формат).

**Что есть в боте (контент-меню):**
| Функция | Статус | Где |
|---------|--------|-----|
| Публикации по теме проекта | ✅ | Быстрый текст, 7 дней прогрева, Интерактивный план, Новость |
| Новости | ✅ | «📰 Новость» → тема → ИИ → превью → публикация |
| Интересные факты | ✅ | «💡 Интересный факт» → тема → пост в стиле факта → превью → публикация |
| Поздравления с праздниками РФ | ✅ | «🎉 Праздник РФ» → выбор праздника (Новый год, 23 февраля, 8 Марта, 9 Мая, День России, День строителя и др.) → пост в канал |
| Поздравления с ДР в личку подписчикам | ✅ | По расписанию 9:00; таблица `clients_birthdays`; сервис `services/birthday_greetings.py` |
| Темы от креативщика | ✅ | Меню АНТОНА «🕵️‍♂️ Темы от Шпиона» (CreativeAgent); раз в 6 ч темы также уходят в топик «Тренды/Сезон» рабочей группы |
| Фото → ИИ описание → согласование → публикация | ✅ | «📸 Фото → Описание → Пост»: тема → загрузка фото → сжатие (1024px) → ИИ описание → превью с кнопками → публикация |

- **Рубрики:** FAQ, кейсы «было/стало», мифы и страшилки, нормы простым языком, новости, праздники.
- **Структура поста:** заход → основная часть (факты из базы) → вывод и советы → CTA (каждый 2–3 пост).
- **CTA:** призыв оставить заявку через бота @Parkhovenko_i_kompaniya_bot.
- **Публикации:** ТЕРИОН, ДОМ ГРАНД, VK, MAX. Кнопки в превью и в черновиках в рабочей группе.
- **Изображения:** генерация без текста на картинке; фото поста сжимаются под формат (compress_image 1024px). Router AI (одного ключа достаточно), опционально Яндекс АРТ.
- **Ограничения:** не придумывать номера законов; не обещать «узаконить всё»; не «обходить закон»; точка входа — бот.

---

## 9. Карта агентов (кратко)

| Агент | Задачи |
|-------|--------|
| **Антон (бот)** | Диалог (RAG), квиз (заявка), быстрые ответы, транскрибация голоса. |
| **Агент-Шпион (Yandex AI)** | Анализ сообщений из чатов ЖК (ID fvt2vnpq2qjdt829em50), JSON: hotness, recommendation, pain_level. Карточки лидов при hotness ≥ 3. |
| **Агент-Антон (Yandex AI)** | Экспертный ответ по контексту (Retrieval), до 500 знаков + дисклеймер + призыв квиза; предварительное заключение по сводке квиза (ID fvtrdfvmv1u84s9rfn5a). Обработчики кнопок в handlers/admin.py. |
| **Content (handlers/content.py + content_agent)** | Черновики постов, серии прогрева, публикация в TG/VK. |
| **LeadHunter + ScoutParser** | Парсинг TG/VK (18 открытых каналов + цели из БД), `last_post_id`, анализ (LeadAnalyzer → ST-1…ST-4), карточки v2 в группу + карточка Юлии (ЖК-заголовок, стадия, автор, проект ответа, кнопки). Управление: `/approve_targets`, `/add_target`, `/spy_discover`. **Планировщик сводок:** обычные лиды → сводка 3 раза в день, горячие → немедленно. |
| **Агент-Продавец** | `_generate_sales_reply` в LeadHunter: Yandex GPT + `hunter_manual.md` → живой текст ответа под стадию боли. Блок `<code>` в карточке Юлии — тап и копировать. **В работе:** 5-шаговый скрипт продаж с State-Machine. |
| **Детектор ЖК** | `_detect_priority_zhk_hot` в `analyzer.py`: Зиларт / Символ / Сердце Столицы / Династия + проблемный контекст → принудительный ST-4 без ожидания ИИ. |
| **Creative Agent** | Идеи для контента по расписанию. |
| **Позже** | Лендинг, оплаты — по отдельным брифам/ТЗ. |

---

## 10. Ссылки и репозиторий

- Репозиторий: `github.com/9435421-ui/parkhomenko-bot`, ветка `main`.
- Бот: @Parkhovenko_i_kompaniya_bot.
- Каналы: ТЕРИОН, ДОМ ГРАНД (ID в config).

При внесении изменений обновляйте этот бриф (разделы 4–5 и при необходимости 2), чтобы следующий запуск агента и ты могли сверяться с актуальным состоянием и планами.
>>>>>>> 7088a20d30a8942893a1c5c26400c6546150a377
