import requests
import os
from datetime import datetime, timedelta
import json

class ContentAgent:
    def __init__(self):
        self.folder_id = os.getenv("FOLDER_ID")
        self.api_key = os.getenv("YANDEX_API_KEY")
        self.endpoint = "https://llm.api.cloud.yandex.net/foundationModels/v1/completion"
        self.brief_content = self._load_brief()

        # Шаблоны поздравлений с днем рождения (без упоминания услуг/перепланировок)
        self.birthday_templates = [
            "Поздравляем вас с днем рождения! Пусть этот день будет наполнен радостью, теплом близких и приятными сюрпризами. Желаем крепкого здоровья, душевного равновесия и исполнения самых заветных желаний.",
            "С днем рождения! Пусть этот особенный день принесет вам море улыбок, тепла от родных и друзей, а также исполнение всех мечтаний. Желаем здоровья, счастья и благополучия на каждый день.",
            "Поздравляем с днем рождения! Пусть этот день будет ярким и незабываемым, наполненным любовью близких и приятными моментами. Желаем крепкого здоровья, семейного тепла и достижения всех поставленных целей.",
            "С днем рождения! Пусть этот праздник принесет вам заряд положительных эмоций, теплые объятия родных и исполнение желаний. Желаем здоровья, счастья и благополучия в вашей жизни.",
            "Поздравляем с днем рождения! Пусть этот день будет особенным, наполненным радостью, теплом и заботой близких. Желаем крепкого здоровья, душевного комфорта и достижения всех мечтаний.",
            "С днем рождения! Пусть этот праздник станет началом новых радостных событий в вашей жизни. Желаем здоровья, счастья, семейного тепла и исполнения самых сокровенных желаний.",
            "Поздравляем с днем рождения! Пусть этот день будет ярким и незабываемым, а каждый новый день приносит новые возможности и радости. Желаем крепкого здоровья и благополучия.",
            "С днем рождения! Желаем вам тепла от близких, радости от маленьких побед и исполнения мечтаний. Пусть этот день станет одним из самых счастливых в вашей жизни."
        ]

    def build_image_prompt(self, post: dict) -> str | None:
        """
        Генерирует текстовый промпт для создания изображения к посту.
        """
        post_type = post.get('type', 'экспертиза')
        title = post.get('title', '')
        body = post.get('body', '')

        prompts = {
            'экспертиза': f"профессиональное фото интерьера, чертежи перепланировки на столе, архитектурный проект, современный стиль, высокое качество, 4k",
            'живой': f"уютная квартира, теплый свет, элементы домашнего декора, счастливая атмосфера дома, без людей, фотореализм",
            'новость': f"современное здание, городская архитектура, документы и печать, официальный стиль, минимализм",
            'приветствие': f"открытая светлая квартира, современный дизайн, гостеприимная атмосфера, панорамные окна"
        }

        # Если есть заголовок, добавляем его в контекст промпта
        base_prompt = prompts.get(post_type, prompts['экспертиза'])
        if title:
            return f"{base_prompt}, тема: {title}"

        return base_prompt

    def _load_brief(self):
        """Загружает базу знаний"""
        try:
            with open("BRIEF_pereplanirovki.md", "r", encoding="utf-8") as f:
                return f.read()
        except FileNotFoundError:
            return "База знаний не найдена. Используйте общие знания по перепланировкам."

    def generate_posts(self, count=7, post_types=None, theme=None):
        """
        Генерирует N постов
        post_types: {'экспертиза': 5, 'живой': 1, 'новость': 1}
        theme: опциональная тема недели (например: "новый год и зимние перепланировки")
        """
        if post_types is None:
            post_types = {'экспертиза': count - 1, 'живой': 1}

        posts = []
        start_date = datetime.now() + timedelta(days=1)
        start_date = start_date.replace(hour=10, minute=0, second=0)

        for post_type, num in post_types.items():
            for i in range(num):
                prompt = self._build_prompt(post_type, theme)
                text = self._call_yandex_gpt(prompt)

                # Парсим ответ: заголовок (опционально), текст, CTA
                title, body, cta = self._parse_response(text)

                post = {
                    'type': post_type,
                    'title': title,
                    'body': body,
                    'cta': cta,
                    'publish_date': start_date + timedelta(days=len(posts)),
                    'image_prompt': self.build_image_prompt({'type': post_type})  # ← добавить
                }
                posts.append(post)

        return posts

    def _build_prompt(self, post_type, theme=None):
        """Формирует промпт для конкретного типа поста"""
        season = self._get_season_context()
        theme_note = f"\nУчитывай тему недели: {theme}" if theme else ""

        prompts = {
            'экспертиза': f"""
Создай пост в стиле "Полезный разбор" для канала «ЛАД В КВАРТИРЕ».
Тема: {theme if theme else 'что-то острое из базы знаний'}

Требования:
- Начни с боли клиента или реального случая (например: "Купили квартиру, а там снесена стена...").
- Объясни последствия простым языком.
- Дай короткий алгоритм: что делать прямо сейчас.
- CTA: предложи пройти быстрый опрос в боте, чтобы передать данные эксперту на проверку.

Формат ответа:
[🔥 Цепляющий заголовок]

[Текст поста]

👉 [CTA к нашему боту-ассистенту @Parkhovenko_i_kompaniya_bot]
""",
            'живой': f"""
Создай "живой" сторителлинг для канала «ЛАД В КВАРТИРЕ».
Контекст: {season}{theme_note}

Требования:
- Никаких штампов типа "Зима — время задуматься". Начни с живой сцены: "Утро, кофе и внезапная мысль: а не расширить ли кухню за счет лоджии?".
- Опиши типичные сомнения клиента (страх штрафов, долгого согласования).
- Расскажи, как мы в «ЛАД В КВАРТИРЕ» делаем этот путь легким.
- CTA: "Опишите свою идею нашему боту-ассистенту @Parkhovenko_i_kompaniya_bot — он соберет данные для бесплатного аудита".

Формат ответа:
[Заголовок-история]

[Текст поста]

👉 [Призыв к боту]
""",
            'новость': f"""
Создай новостной пост в стиле "Важные изменения" для канала «ЛАД В КВАРТИРЕ».

Требования:
- Объявление об изменении норм или важном кейсе без канцелярита.
- Кратко: что изменилось и чем это реально грозит (или помогает) жильцам.
- Никаких "Зима — время перемен". Сразу к делу: "Жилинспекция ужесточила правила...".
- CTA: "Проверьте, попадает ли ваша перепланировка под новые правила, через нашего бота-ассистента".

Формат ответа:
[📢 Свежие новости]

[Текст поста]

[Что это значит для жильцов]

👉 [CTA]
""",
            'поздравление': f"""
Напиши короткое искреннее поздравление с днём рождения.

Кого поздравляем: {theme_note or 'друг'}

Требования:
- 60-100 слов
- Тёплые пожелания счастья, здоровья, радости
- Пожелание уюта и тепла в доме
- Простой дружелюбный язык
- БЕЗ упоминания работы, услуг, бизнеса

Формат:
🎂 [Имя]

[Поздравление с пожеланиями]
"""
        }

        return prompts.get(post_type, prompts['экспертиза'])

    def _get_season_context(self):
        """Определяет сезонный контекст"""
        month = datetime.now().month

        contexts = {
            (12, 1, 2): "Зима: снег завалил Москву, промерзающие окна, отключения отопления, сосульки на козырьках, утепление",
            (3, 4, 5): "Весна: отключение горячей воды, подготовка к ремонтному сезону, пора задуматься о перепланировке",
            (6, 7, 8): "Лето: пик ремонтного сезона, активные перепланировки",
            (9, 10, 11): "Осень: включение отопления, завершение ремонтов перед зимой"
        }

        for months, context in contexts.items():
            if month in months:
                return context
        return contexts[(12, 1, 2)]

    def _call_yandex_gpt(self, user_prompt, mode="default"):
        """Вызов YandexGPT API"""

        if mode == "greeting":
            system_prompt = """
Ты пишешь короткие личные поздравления.

Твоя задача — писать тёплые человеческие тексты без продаж и без упоминания услуг, компаний, ботов, рекламы, акций и скидок.
Строго запрещено упоминать перепланировку, ремонт, юридические услуги, консультации, кредиты, долги, бизнес, команду экспертов, Telegram-ботов и любые предложения «обратиться», «заказать», «получить консультацию».

Пиши простым дружелюбным языком, как человек человеку.
Не добавляй ссылки, хэштеги, GIF, эмодзи кроме указанного в формате.
"""
        else:
            system_prompt = f"""
Ты — креативный копирайтер Telegram-канала «ЛАД В КВАРТИРЕ».

Твоя задача — писать вовлекающие, легкие и интересные посты о перепланировках. Мы не просто даем сухую инфомацию, мы рассказываем истории и решаем боли клиентов.

КРИТИЧЕСКИ ВАЖНО:
1. СТИЛЬ: Живой, человеческий, сторителлинг. Избегай канцелярита, сложных конструкций и скучных вступлений про погоду (если они не несут смысла).
2. СТРУКТУРА: Цепляющий заголовок -> История или Острая проблема -> Простое решение -> Понятный призыв к действию (CTA).
3. CTA: Мы ведем людей в наш БОТ-АССИСТЕНТ. Задача бота — собрать данные пользователя через быстрый квиз, чтобы эксперт мог перезвонить с готовым решением.
4. НИКАКОЙ "ИИ-КОНСУЛЬТАЦИИ": Не пиши, что бот ответит на вопросы или проконсультирует. Пиши, что бот поможет быстро оформить заявку, чтобы получить разбор от живого эксперта.

База знаний:
{self.brief_content[:3000]}

Пиши так, чтобы пост хотелось дочитать до конца. Используй эмодзи умеренно, для акцентов.
"""

        headers = {
            "Content-Type": "application/json",
            "Authorization": f"Api-Key {self.api_key}",
            "x-folder-id": self.folder_id
        }

        payload = {
            "modelUri": f"gpt://{self.folder_id}/yandexgpt/latest",
            "completionOptions": {
                "stream": False,
                "temperature": 0.7,
                "maxTokens": 2000
            },
            "messages": [
                {"role": "system", "text": system_prompt},
                {"role": "user", "text": user_prompt}
            ]
        }

        response = requests.post(self.endpoint, headers=headers, json=payload, timeout=30)
        response.raise_for_status()

        result = response.json()
        return result['result']['alternatives'][0]['message']['text']

    def _parse_response(self, text):
        """Парсит ответ LLM на title, body, cta"""
        lines = text.strip().split('\n')

        # Ищем CTA (строки с 👉 или "CTA:")
        cta_line = None
        for i, line in enumerate(lines):
            if '👉' in line or 'CTA:' in line.upper():
                cta_line = i
                break

        if cta_line:
            cta = '\n'.join(lines[cta_line:]).strip()
            body_lines = lines[:cta_line]
        else:
            cta = ""
            body_lines = lines

        # Первая строка может быть заголовком, если она короткая
        title = ""
        if body_lines and len(body_lines[0]) < 100:
            title = body_lines[0].strip('#').strip()
            body_lines = body_lines[1:]

        body = '\n'.join(body_lines).strip()

        return title, body, cta

    def _sanitize_greeting(self, text: str) -> str:
        """Убирает слишком продающие поздравления (страховка)"""
        banned = [
            "ремонт", "перепланиров", "услуг", "консультац", "бот",
            "телеграм", "команда экспертов", "скидк", "предложение",
            "закажите", "обратитесь", "юридическ", "кредит", "долг"
        ]
        lower = text.lower()
        if any(word in lower for word in banned):
            # Фолбэк — нейтральное поздравление без продаж
            return (
                "Пусть этот день будет для вас тёплым и радостным. "
                "Желаем здоровья, душевного комфорта, уюта дома и людей рядом, "
                "с которыми хочется делиться хорошими моментами."
            )
        return text

    def generate_greeting_post(self, person_name, date, occasion='день рождения'):
        """
        Генерирует персональный поздравительный пост

        Args:
            person_name: Имя человека (может быть пустым)
            date: Дата в формате DD.MM или DD.MM.YYYY
            occasion: Повод для поздравления ('день рождения', 'Новый год', etc.)

        Returns:
            dict: {'title': str, 'body': str, 'cta': str, 'type': 'поздравление'}
        """
        display_name = person_name if person_name else "наш подписчик"

        prompt = f"""
Создай короткое искреннее поздравление для личного сообщения в Telegram.

Повод: {occasion}
Имя: {display_name}
Дата: {date}

Требования:
- Тёплое персональное поздравление (60–100 слов)
- Пожелания здоровья, радости, душевного комфорта, уюта дома
- Простой дружелюбный язык, без высокопарного канцелярита
- Строго запрещено упоминать услуги, ремонт, перепланировку, консультации, ботов, компании, акции, скидки и любые продажи.
- Не добавляй CTA, ссылки, хэштеги, GIF и призывы что-то заказать или узнать.

Формат ответа:
🎂 {display_name}

[Поздравление с пожеланиями]
"""

        text = self._call_yandex_gpt(prompt, mode="greeting")
        text = self._sanitize_greeting(text)

        # Для поздравления title можно не использовать, всё в body
        title = ""
        body = text.strip()
        cta = ""  # никаких CTA в поздравлении

        return {
            'type': 'поздравление',
            'title': title,
            'body': body,
            'cta': cta
        }

    def generate_birthday_congrats_template(self, person_name: str | None, date: str):
        """
        Генерирует поздравление с днем рождения из шаблонов (без ИИ)

        Args:
            person_name: Имя человека или None
            date: Дата в формате DD.MM или DD.MM.YYYY

        Returns:
            dict: {'title': str, 'body': str, 'cta': str, 'type': 'поздравление'}
        """
        import random

        # Выбираем случайный шаблон
        congrats_text = random.choice(self.birthday_templates)

        # Формируем имя для поздравления
        display_name = person_name if person_name else "наш подписчик"

        # Формируем тело сообщения
        body = f"🎂 {display_name}\n\n{congrats_text}"

        return {
            'type': 'поздравление',
            'title': '',
            'body': body,
            'cta': ''
        }

    def generate_welcome_post(self, person_name: str | None = None):
        """
        Генерирует приветственное сообщение-визитку для потенциального клиента.

        Args:
            person_name: Имя человека или None

        Returns:
            dict: {'title': str, 'body': str, 'cta': str, 'type': 'приветствие'}
        """
        display_name = person_name if person_name else "новый подписчик"

        prompt = f"""
Создай приветственное сообщение для Telegram-канала «ЛАД В КВАРТИРЕ».

Адресат: {display_name}

Требования:
- 80–120 слов
- Кратко представить канал и пользу: помощь с вопросами перепланировки, рисками, узакониванием
- 1–2 простых примера ситуаций, когда к нам можно обратиться (перенос стен, объединение кухни и комнаты, перепланировка с мокрыми зонами)
- Один аккуратный CTA к нашему боту-ассистенту @Parkhovenko_i_kompaniya_bot
- Спокойный, дружелюбный тон

Формат ответа:
👋 {display_name}

[Текст приветствия 2–3 абзаца с CTA в конце]
"""

        text = self._call_yandex_gpt(prompt, mode="default")  # обычный продающий режим
        title, body, cta = self._parse_response(text)

        return {
            'type': 'приветствие',
            'title': title,
            'body': body,
            'cta': cta
        }
